{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-open-ptrades-pnl-trap-20260910\",\"action\":\"OPEN_MANUAL\",\"actor\":\"50cal\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"title\":\"paper_trades is not real-money P&L for a LIVE tag: never-filled orders inflate it\",\"problem_statement\":\"The live path writes SIMULATED paper_trades rows under the LIVE tag, including for orders that never filled on the exchange. Any P&L read from paper_trades for a live tag therefore counts trades real money never got, and can read positive while the book is losing. Measured 2026-09-10 on the mmsell contest-cap live canary, scoped to its epoch: paper_trades read +2.56 USD (n=194) while the exchange's own realized P&L for the same tag and window was -1.41 USD (settled=123); the difference was 70 tickers whose live order never filled, worth +4.54 USD. On the subset that DID fill the simulator read -1.97 USD vs -1.41 USD real, so the apparent paper edge is selection at the fill, not a modelling error. Fill rate over the epoch: 136 of 180 tickers ordered. This is the same class of error as the open-position count, where the bound is enforced by count_live_book_open and NOT by a paper_trades row count; it has now produced a materially wrong operator report twice. NOT a safety defect: the max_daily_loss breaker (LiveExecutor._daily_loss_hit -> repository.live_realized_pnl_today) already reads positions.realized_pnl, and the live dashboard already attributes via live_orders.strategy -> fills -> newest positions snapshot (kalshi_bot/livedash/legs.py). The gap is that there is no canonical, ops-reachable read of live realized P&L and open count BY TAG, so every ad-hoc analysis re-derives it and the wrong source is the easy one. Asks: (a) a named, ops-callable read for live realized P&L and live open count by tag, using the same attribution chain the dashboard and the breaker use; (b) make the provenance visible where a live tag appears in paper_trades, so a simulated row for a live tag cannot be mistaken for a real fill.\",\"classification\":\"DATA\",\"owner_role\":\"LIVE_OPS\",\"severity\":\"HIGH\",\"priority\":\"P2\",\"reason\":\"Opened by Live Ops after a real-money report was given to the operator from the wrong source and corrected against the exchange's own realized P&L. Recurrence of a known trap, so it belongs in durable state rather than a session note.\"}}"}, "id": "cc-iss-open-1"}