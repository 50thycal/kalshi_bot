{"type":"db","id":"limm-cat-2","sql":"SELECT kv.k AS field, count(*)::text AS n, min(kv.v) AS example FROM incentive_programs p, LATERAL jsonb_each_text(to_jsonb(p.extra_params_json)) AS kv(k, v) WHERE p.superseded_at IS NULL AND jsonb_typeof(to_jsonb(p.extra_params_json)) = 'object' GROUP BY kv.k ORDER BY count(*) DESC LIMIT 25"}
