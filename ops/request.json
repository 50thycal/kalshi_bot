{"type":"db","sql":"select market_ticker, quantity, market_exposure, captured_at from (select distinct on (market_ticker) market_ticker, quantity, market_exposure, captured_at from positions order by market_ticker, captured_at desc) p where coalesce(quantity,0) <> 0 and captured_at > now() - interval '3 hours' order by captured_at desc","max_rows":30,"id":"lo-x3-db-live2"}
