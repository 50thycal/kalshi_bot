{"type":"db","id":"cc-pf1","sql":"select strategy, count(*) as n, min(created_at) as first_at, max(created_at) as last_at, count(*) filter (where settled_at is not null) as settled from paper_trades where strategy like 'Gmmsell%' group by strategy order by strategy","max_rows":50}
