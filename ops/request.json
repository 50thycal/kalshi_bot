{"type": "db", "sql": "select category, status, count(*) as n, min(created_at)::date::text as first_asked, max(created_at)::date::text as last_asked, left(string_agg(distinct capability, ' | '), 700) as capabilities from evo_tickets group by category, status order by n desc", "max_rows": 80, "id": "tickets-rollup-1"}
