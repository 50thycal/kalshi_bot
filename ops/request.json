{"type":"db","id":"limm-watch-8","sql":"SELECT created_at::text AS created, updated_at::text AS updated, strategy, market_ticker, side, limit_price::text AS px, status, coalesce(cancel_reason,'-') AS reason FROM live_orders WHERE created_at > now() - interval '14 hours' ORDER BY created_at DESC LIMIT 40"}
