{"type":"db","sql":"SELECT decision, cap_bound, telemetry_status, count(*) AS n, count(DISTINCT kalshi_order_id) AS orders, min(decided_at)::text AS first_seen, max(decided_at)::text AS last_seen FROM live_order_queue_decisions GROUP BY 1,2,3 ORDER BY 4 DESC","max_rows":60,"id":"qac-accrual-1"}
