{"type":"db","id":"lo359-tape-2","max_rows":40,"sql":"select 'perp_market_snapshots' as tbl, count(*) as total_rows, max(captured_at)::text as latest, count(*) filter (where captured_at > now() - interval '24 hours') as last_24h, count(*) filter (where captured_at > now() - interval '7 days') as last_7d from perp_market_snapshots union all select 'perp_orderbook_snapshots', count(*), max(captured_at)::text, count(*) filter (where captured_at > now() - interval '24 hours'), count(*) filter (where captured_at > now() - interval '7 days') from perp_orderbook_snapshots union all select 'perp_funding_observations', count(*), max(captured_at)::text, count(*) filter (where captured_at > now() - interval '24 hours'), count(*) filter (where captured_at > now() - interval '7 days') from perp_funding_observations union all select 'perp_collector_cycles', count(*), max(started_at)::text, count(*) filter (where started_at > now() - interval '24 hours'), count(*) filter (where started_at > now() - interval '7 days') from perp_collector_cycles"}
