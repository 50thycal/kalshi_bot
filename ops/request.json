{"type":"db","sql":"select status, count(*) as n, count(kalshi_order_id) as with_koid, sum(quantity) as qty from live_orders where strategy = 'Cmmsell10' and action = 'buy' group by 1 order by 1","max_rows":10,"id":"db-koid"}
