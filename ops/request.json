{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-resolve-xos7-20260827\",\"action\":\"RESOLVE\",\"actor\":\"cal\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"issue\":\"XOS-000007\",\"resolution_summary\":\"Resolved by PR 260 (merged 2026-08-27), in the four parts the proposed fix named. (1) RETIRED THE INSTRUCTION: every standing instruction to force-refresh ops is gone from CLAUDE.md, docs/OPS_RUNBOOK.md, the kalshi-probe-builder skill and its probe-and-ops reference, and two stale operational notes in docs/RESEARCH_JOURNAL.md and docs/IDEA_MODEL_20260711_run2.md. A further instruction telling sessions to copy a corrected script onto ops and run it from there was removed too: since XOS-000005 the runner's freshness guard refuses that path outright, so it documented something that now fails closed. (2) BOOTSTRAP TREATED AS FINAL: the 2026-08-24 refresh was the last routine one, and the runbook now says so. (3) CAPABILITY REMOVED: refs/heads/ops carries the repository ruleset ops-transport-guard - enforcement active, no bypass actors, rules deletion and non_fast_forward and nothing else. Its desired state is checked in at .github/rulesets/ops-transport-guard.json and applied idempotently by the Ops Branch Protection workflow, so the protection is reviewable and reproducible rather than a hand-clicked setting. The ruleset deliberately omits pull_request, required_status_checks, required_linear_history, required_signatures, creation and update, and a test asserts their absence - any of them would disable the transport, whose ordinary traffic is unreviewed direct fast-forwards. (4) RARE EXCEPTION DEFINED: the runbook section 'Protecting the ops branch' gives the deliberate maintenance procedure for a workflow-file change - idle/noop channel check, backup BRANCH at the recorded SHA, force-with-lease against that exact SHA rather than a bare -f, post-change round-trip validation including the OPS_RUNNER_CODE_SOURCE attestation, and recovery from the backup. Both mechanics learned on 2026-08-24 are written down: a force-push onto an existing commit does not trigger the workflow, and tag pushes are rejected by the sandbox transport so a backup branch is the reliable archive form. Validation PASSED on all four pre-registered checks, including the exact GH013 refusal text for a controlled force update and a full request/result round trip measured under the live protection. Two follow-ups outside this ticket: the temporary OPS_ADMIN_TOKEN PAT must be deleted and revoked now that apply and validation are complete (the ruleset stays enforced without it), and XOS-000010 was opened separately for the platform-command receipt visibility defect found while working this one.\"}}"}, "id": "lo7-resolve-set"}