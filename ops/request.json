{"type":"db","sql":"select date_trunc('hour', created_at) as hr, count(*) filter (where status = 'rejected') as rejected, count(*) filter (where status = 'filled') as filled, count(*) as total from live_orders where strategy = 'Cmmsell10' and action = 'buy' group by 1 order by 1","max_rows":50,"id":"db-rej-time"}
