{"type":"db","sql":"SELECT date_trunc('day', created_at)::date::text AS day, strategy, count(*) AS refusals FROM skipped_trades WHERE reason LIKE '%open_cap%' AND created_at > now() - interval '10 days' GROUP BY 1,2 ORDER BY 1 DESC, 3 DESC","max_rows":60,"id":"qac-cap-hist-1"}
