{"type":"db","sql":"select coalesce(p.strategy,'?') as strategy, count(*) filter (where p.status='open') as pos_open, count(*) filter (where p.status<>'open') as pos_closed, min(p.opened_at)::text as oldest_open from paper_positions p group by 1 order by 2 desc limit 15","max_rows":20,"id":"pos-scope-1"}
