{"type":"db","sql":"select date_trunc('hour', created_at) as hr, count(*) filter (where strategy = 'mmsell10') as mmsell10, count(*) filter (where strategy like '%mmsell%') as all_mmsell from paper_trades where created_at > now() - interval '8 hours' group by 1 order by 1","max_rows":40,"id":"db-mm10-hourly"}
