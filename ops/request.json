{"type":"db","id":"cc-meta-shape-1","sql":"select split_part(event_ticker,'-',1) as series, count(*) n, count(distinct (close_time at time zone 'UTC')::date) dates, min(close_time) mn, max(close_time) mx from mmsell_settlement_meta where close_time > now() - interval '14 days' group by 1 order by 2 desc limit 25"}
