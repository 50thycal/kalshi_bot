{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\": \"xos-worker-outage-20260830\", \"action\": \"OPEN_MANUAL\", \"actor\": \"cal\", \"actor_role\": \"LIVE_OPS\", \"schema_version\": 1, \"payload\": {\"title\": \"Worker crash-looped 16h51m on an invalid config set through the ops env transport\", \"problem_statement\": \"The trading worker crash-looped and was fully down for 16h51m (2026-08-30T18:37:11Z to 2026-08-31T11:28:35Z). Scope was worker-wide, not one book: zero bot_runs, zero mmsell candidate ticks, zero paper trades across all strategies, and zero live orders. No money was at risk (no orders were placed; open positions settle exchange-side regardless), but all scanning and all evidence collection stopped, leaving a hole in the mmsell10 live canary's epoch-2 evidence window.\\n\\nROOT CAUSE: LIVE_SHAPE_PROBE was cleared through the ops env transport by setting it to an empty string. The field is a bool; pydantic rejects '' with 'Input should be a valid boolean', config is fail-closed, and setting any var redeploys the worker. The worker therefore refused to start on every restart. Reproduced locally: '' is rejected, 'false' parses. The empty-string clear is the documented pattern for the STRING command-transport vars (EXPERIMENT_OS_ISSUE_COMMAND); it was applied to a bool without checking the field type.\\n\\nWHY IT LASTED 17 HOURS, which matters more than the typo: (1) the env change was pushed and the ops result 'redeploy triggered' was treated as success without verifying the worker actually booted; (2) the boot error 'invalid configuration; refusing to start' put the failing field in extra_fields, which the ops log view drops, so the log named the symptom and hid the cause; (3) nothing monitored worker liveness, so the outage surfaced only when a human asked an unrelated question about fill rates.\\n\\nREMEDIATION SHIPPED: PR 297 makes scripts/railway_env.py validate every value against the real Settings field before touching the network, fail-closed like the existing allowlist and size checks, so an unparseable value can no longer reach Railway. PR 297 also puts the failing field name in the boot error message itself, with field names and reasons only and never the rejected values, because those logs are read back onto a public branch and a rejected value can be a credential. PR 298 adds a scheduled worker-liveness alarm that reads bot_runs every 15 minutes from outside the worker and fails the job when the newest row is older than 30 minutes.\\n\\nSTILL OPEN: the mmsell10 canary's epoch-2 evidence now contains a 17-hour gap that must be recorded rather than silently pooled over.\", \"classification\": \"OPS\", \"owner_role\": \"LIVE_OPS\", \"severity\": \"HIGH\", \"priority\": \"P2\", \"reason\": \"Full worker outage on the live-canary path, self-inflicted through the ops env transport and undetected for 17 hours. Recorded as durable state because the evidence gap affects an active live canary's epoch and because the detection failure, not the typo, is the reusable lesson.\"}}"}, "id": "xos-outage-open"}