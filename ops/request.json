{"type":"db","sql":"select strategy, count(*) as n, max(created_at)::text as last_order, min(created_at)::text as first_order from live_orders where created_at > now() - interval '4 days' group by 1 order by 4 desc","max_rows":30,"id":"live-activity-1"}
