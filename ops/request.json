{"type":"db","id":"cc-pf2","sql":"select strategy, count(*) as n, count(*) filter (where status='closed') as closed, min(created_at) as first_at, max(created_at) as last_at from paper_trades where strategy like 'Gmmsell%' or strategy like 'mmsell10%' group by strategy order by strategy","max_rows":60}
