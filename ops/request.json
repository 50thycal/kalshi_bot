{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-t1-valplan-20260822\",\"action\":\"RECORD_VALIDATION_PLAN\",\"actor\":\"claude-code\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"issue\":\"XOS-000004\",\"advance\":false,\"validation_plan\":\"The bar is a CAPABILITY demonstration, not a code review: an operator with only the ops channel must be able to name the first funnel stage that reaches zero, for a book they have never looked at, without reading source and without a database query. Four checks, each performed against PRODUCTION output after the repair deploys. (1) EMPTY-SERIES WARNING FIRES: with the current configuration unchanged, a logs read shows a warning naming the configured series that returned zero open markets on the most recent cycle. FREEZE is the natural subject because all seven of its series are known-empty, so a correct implementation must warn on all seven; if it warns on none, the repair has not landed. (2) FUNNEL IS READABLE: the same logs read shows the per-cycle counters (markets, commodity, eligible, pinned, opened, and each skip reason) as values an operator can read, not as a bare message. (3) THE DISCRIMINATION ACTUALLY WORKS: for one book seeing zero markets and one book seeing markets and rejecting them, production output alone distinguishes the two cases and localises the first zero to a named stage. The second half matters as much as the first - a repair that only proves the failing case does not prove the capability. (4) NO REGRESSION IN SIGNAL-TO-NOISE: a normally-quiet series does not produce a warning on every cycle, and the trading cycle's log volume is not materially increased. Closing evidence must cite the ops result ids for the reads above. If check (3) cannot be demonstrated, the ticket returns to INVESTIGATING rather than resolving - the observed symptom being gone is not the same as the diagnosis being possible next time.\",\"reason\":\"pre-registering what would count as proof, before anyone builds the repair\"}}"}, "id": "lo-t1-cmd5"}