{"type":"db","sql":"select split_part(market_ticker,'-',1) as series, status, count(*) as n, min(created_at)::text as first_seen, max(created_at)::text as last_seen from live_orders where strategy='Cmmsell10' and created_at > now() - interval '6 hours' group by 1,2 order by 1,2","max_rows":40,"id":"shard3-live-1"}
