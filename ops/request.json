{"type": "env", "service": "main", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\": \"xos20-evidence-utcgap-1\", \"action\": \"ADD_EVIDENCE\", \"actor\": \"claude-code\", \"actor_role\": \"RESEARCH_LAB\", \"payload\": {\"issue\": \"XOS-000020\", \"evidence_type\": \"COMMIT\", \"source_ref\": \"576bcfa\", \"summary\": \"SEPARATE GAP in the merged mechanism, found while building the forward test and NOT fixed there. The contest counter rides open_positions_settlement_summary, which filters open positions to the candidate's own UTC settlement DATE (day_start <= close_time < day_end). A contest's markets do not all share one date: an F5 total closes about an hour into a game and the full-game total about three, so a start around 18:30 ET puts the early legs before UTC midnight and the late ones after. On such a slate the second leg is counted against a different day's budget and the contest cap silently does not fire - precisely on the late-evening games this ticket's own drawdown came from (26SEP02 NYYLAA was a 21:38 start). The failure is invisible: skipped_contest_cap simply does not increment, so the cap looks like it had nothing to refuse. The event RUNG cap has always had the same scoping and the same exposure; it is not new, but the contest cap is the first to span series and so the first for which straddling is the common case rather than the edge. A fix is a date-unscoped read keyed on the contest (the open book is bounded by mmsell_max_open_positions, so reading all of it is cheap). NOT bundled into the forward test: changing the read would change the treatment arm mid-registration.\"}, \"schema_version\": 1}"}, "id": "xos20-ev-2"}