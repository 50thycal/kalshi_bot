{"type":"db","sql":"select captured_at::text as at, market_ticker, (select jsonb_agg(k order by k) from jsonb_object_keys(raw_json) k)::text as keys from market_snapshots order by captured_at desc limit 3","max_rows":5,"id":"shard-idx-2"}
