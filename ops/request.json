{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-t1-open-20260822\",\"action\":\"OPEN_MANUAL\",\"actor\":\"claude-code\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"title\":\"Series-addressed books can see zero markets indefinitely with no operator-visible warning, and their funnel counters are unreadable in production\",\"classification\":\"OPS\",\"owner_role\":\"LIVE_OPS\",\"severity\":\"MEDIUM\",\"priority\":\"P2\",\"problem_statement\":\"CROSS-BOOK OPERATIONAL OBSERVABILITY DEFECT. Any book that addresses its universe by a configured list of Kalshi series tickers can fetch zero open markets on every cycle, forever, and produce output indistinguishable from a healthy book. Two independent gaps combine. (1) NO EMPTY-SERIES WARNING: the fetch loop calls get_markets(status=open, series_ticker=S) per configured series; a series that matches nothing on the venue returns HTTP 200 with an empty list, which is not an exception, so the existing except-and-warn path never fires and nothing is logged. A mistyped, renamed, delisted or never-validated series ticker is therefore silent by construction. (2) FUNNEL COUNTERS UNREADABLE: the per-cycle counters that would localise the failure (markets seen, classified, eligible, pinned, opened, skipped_illiquid, skipped_discount, skipped_price) are emitted as structured log ATTRIBUTES via log_event/JsonFormatter, but the ops logs channel queries only timestamp, message and severity, so an operator sees the bare message text and none of the numbers. Combined effect: an operator cannot tell a book that is finding nothing from a book that is finding candidates and rejecting them, and cannot identify which funnel stage first reaches zero, from production-visible output alone. This is a diagnostic-capability defect in shared operational tooling, not a property of any one strategy.\",\"reason\":\"Opened from a Live Ops runtime observation during the XOS-000003 diagnosis. It is deliberately NOT part of that ticket: XOS-000003 is a strategy conclusion about one experiment and has been transferred to Research Lab, whereas this defect affects every series-addressed execution path and is repairable without touching any experiment contract. It is also outside the Control Tower candidate-detector surface (issue-candidates lists only the two live-canary gate blockers), so OPEN_MANUAL rather than OPEN_CANDIDATE is the correct door.\"}}"}, "id": "lo-t1-cmd1"}