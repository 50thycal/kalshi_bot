{"type": "db", "sql": "with t as ( select split_part(pt.market_ticker,'-',1) ser, split_part(pt.market_ticker,'-',2) contest, pt.market_ticker tk, pt.strategy bk, case when pt.side='no' then 100-pt.assumed_price else pt.assumed_price end ypx, pt.pnl::float pnl, pt.closed_at cl, pt.created_at cr, extract(epoch from (pt.closed_at - pt.created_at))/3600.0 hold from paper_trades pt where pt.status in ('settled','closed_sl') and not coalesce(pt.legacy,false) and pt.pnl is not null and pt.strategy not in (select twin_tag from live_paper_twins) and split_part(pt.market_ticker,'-',1) in ('KXALBUMEQUIV','KXPUREALBUMS','KXRT','KXRANKLISTSONGSPOTUSA', 'KXNETFLIXRANKSHOW','KXTOPMODEL') ), a as (select * from t where ser='KXALBUMEQUIV') select 'A_family'::text sec, ser::text k, count(*)::text n, count(distinct tk)::text mkts, count(distinct contest)::text cnts, round(sum(pnl)::numeric,2)::text pnl, round((100*sum(pnl)/count(*))::numeric,2)::text cpt, round(avg(ypx)::numeric,1)::text entry, round((100.0*count(*) filter (where pnl<0)/count(*))::numeric,1)::text losspct from t group by 2 union all select 'B_contest', contest, count(*)::text, count(distinct tk)::text, count(distinct bk)::text, round(sum(pnl)::numeric,2)::text, round((100*sum(pnl)/count(*))::numeric,2)::text, round(avg(ypx)::numeric,1)::text, round((100.0*count(*) filter (where pnl<0)/count(*))::numeric,1)::text from a group by 2 union all select 'C_band', case when ypx<=7 then 'a_<=7' when ypx<=11 then 'b_08-11' when ypx<=20 then 'c_12-20' else 'd_21+' end, count(*)::text, count(distinct tk)::text, count(distinct contest)::text, round(sum(pnl)::numeric,2)::text, round((100*sum(pnl)/count(*))::numeric,2)::text, round(avg(ypx)::numeric,1)::text, round((100.0*count(*) filter (where pnl<0)/count(*))::numeric,1)::text from a group by 2 union all select 'D_book', bk, count(*)::text, count(distinct tk)::text, count(distinct contest)::text, round(sum(pnl)::numeric,2)::text, round((100*sum(pnl)/count(*))::numeric,2)::text, round(avg(ypx)::numeric,1)::text, round((100.0*count(*) filter (where pnl<0)/count(*))::numeric,1)::text from a group by 2 union all select 'E_hold', case when hold is null then 'zz' when hold<1 then 'a_<1h' when hold<24 then 'b_1-24h' when hold<72 then 'c_24-72h' else 'd_72h+' end, count(*)::text, count(distinct tk)::text, count(distinct contest)::text, round(sum(pnl)::numeric,2)::text, round((100*sum(pnl)/count(*))::numeric,2)::text, round(avg(ypx)::numeric,1)::text, round((100.0*count(*) filter (where pnl<0)/count(*))::numeric,1)::text from a group by 2 union all select 'F_week', to_char(date_trunc('week',cl),'MM-DD'), count(*)::text, count(distinct tk)::text, count(distinct contest)::text, round(sum(pnl)::numeric,2)::text, round((100*sum(pnl)/count(*))::numeric,2)::text, round(avg(ypx)::numeric,1)::text, round((100.0*count(*) filter (where pnl<0)/count(*))::numeric,1)::text from a group by 2 order by 1,2", "max_rows": 300, "id": "rl-albumequiv-1", "actor": "claude-research-lab", "purpose": "KXALBUMEQUIV loss cell: family, contest, band, book, hold, week"}