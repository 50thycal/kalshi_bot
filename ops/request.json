{"type": "env", "id": "dash-issue-probe-evid", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\": \"lo-livedash-probe-20260903\", \"action\": \"ADD_EVIDENCE\", \"actor\": \"claude-code\", \"actor_role\": \"LIVE_OPS\", \"schema_version\": 1, \"payload\": {\"issue\": \"XOS-000018\", \"evidence_type\": \"OPS_RESULT\", \"summary\": \"livedash_probe run against the deployed dashboard 2026-09-03T17:05Z (ops id livedash-probe-1), sequential, from the Actions runner. All eight routes returned 200: /healthz 93ms, / 78ms 60KB, /api/runs?view=selector 161ms 11KB, /api/runs/Dmmsell10_pt4 741ms 376KB, .../series 1369ms 312KB, .../orders 98ms 77KB, .../events 147ms 27KB, /api/runs 4108ms 21KB. The operator's failing route (/api/runs/<tag>) did NOT reproduce and is not inherently slow at 741ms, so the reported failure is conditional - network, concurrency or transient - rather than a fixed cost. It is however the largest payload on the page at 376KB. The one route genuinely slow is /api/runs at 4108ms, 25x any other; it rebuilds both legs of every recorded pair including retired ones, which is WS-009 open decision D1 (cache or trim the run list), deferred there as not urgent and now measured as the slowest thing on the page. It is phase 4 and nothing waits on it, so it is not what broke the card. METHOD LIMITATION worth recording: the per-stage timing lines this PR added could not be read, because pushing an ops request redeploys the service and rotates its logs - observed on livedash (deploy 17:04:30 after the probe push) and on main (deploy 17:08:16 after a read-only logs push). Any action driven through the ops channel therefore destroys the log evidence it was meant to produce. Needs separate investigation and a Railway watch-path change the operator owns.\", \"source_ref\": \"https://github.com/50thycal/kalshi_bot/pull/327\"}}"}}