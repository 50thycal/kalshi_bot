{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-t1-ev-counters-20260822\",\"action\":\"ADD_EVIDENCE\",\"actor\":\"claude-code\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"issue\":\"XOS-000004\",\"evidence_type\":\"OPS_RESULT\",\"summary\":\"THE COUNTERS EXIST BUT ARE UNREACHABLE FROM PRODUCTION-VISIBLE OUTPUT. The book already computes exactly the funnel a diagnosis needs and passes it to log_event as keyword fields; JsonFormatter merges them into the emitted JSON line. They do not survive the read path: the ops logs channel's GraphQL query requests only timestamp, message and severity from deploymentLogs, so every structured field is dropped before an operator ever sees it, and the result is the bare string 'freeze book'. Attribute-level filtering is not a workaround either - a logs request filtered on an attribute predicate came back with the entire unfiltered stream (weather market, weather cycle, live cycle, polymarket index and so on), i.e. the predicate was ignored rather than applied. AFFECTED PATH, stated generally so this is not read as one book's problem: every book whose universe is addressed by a configured series list and fetched with get_markets(series_ticker=...) shares both gaps. FREEZE is the observed instance, not the scope. Any book using this addressing style, and any future one, fails the same way and produces the same undiagnosable output.\",\"source_ref\":\"ops/results/lo-x3-logs-attr.txt\"}}"}, "id": "lo-t1-cmd3"}