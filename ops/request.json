{"type": "env", "id": "dash-issue-evid-1", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\": \"lo-livedash-evid-20260903\", \"action\": \"ADD_EVIDENCE\", \"actor\": \"claude-code\", \"actor_role\": \"LIVE_OPS\", \"schema_version\": 1, \"payload\": {\"issue\": \"XOS-000018\", \"evidence_type\": \"OBSERVATION\", \"summary\": \"Checked via the ops channel on 2026-09-03. livedash service logs (dash-slow-logs-1) show GET /api/runs/<twin_tag> logging 200, then 'request failed', then 503, twice within three seconds; the traceback names server.py:165 _route -> _json -> _send:90 wfile.write(body) BrokenPipeError, with end_headers() at :88 having already succeeded - a peer that was already gone, not an internal fault. Every other route the page requests returned 200 in the same window. Row counts (dash-slow-rows-1) rule out the obvious cost drivers: 2158 parity events for this pair (21792 across all pairs), epoch opened 2026-09-02, 286 paper trades, 136 live orders. /series loads the FULL tick tape and still returns 200, so mark loading is not the driver either. The cost driver remains unidentified because the service logs no request durations; PR #327 adds per-request and per-stage timing plus an ops-runnable probe that times the deployed routes, which is the measurement needed to name it. A wider log pull (dash-slow-logs-2) returned only startup lines because a redeploy had rotated the window. Not reachable from a Claude session directly: sandbox egress refuses *.up.railway.app at the proxy (403 to CONNECT). Read-only surface, so no exposure consequence; the cost is that the live book cannot be observed.\", \"source_ref\": \"https://github.com/50thycal/kalshi_bot/pull/327\"}}"}}