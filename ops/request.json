{"type":"db","id":"et-ws019-21","sql":"select date_trunc('hour', at) hr, count(*)::text n, (array_agg(distinct left(coalesce(detail,''),100)))[1] sample from execution_collector_events where kind='poll_failed' and at > now() - interval '144 hours' group by 1 order by 1 desc limit 24"}
