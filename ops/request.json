{"type":"db","sql":"select strategy, count(*) as rows, max(created_at) as last_row from paper_trades where strategy like '%mmsell%' group by 1 order by 3 desc nulls last","max_rows":60,"id":"db-mm-last"}
