{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-t2-fix-20260822\",\"action\":\"PROPOSE_FIX\",\"actor\":\"claude-code\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"issue\":\"XOS-000005\",\"proposed_fix\":\"Reconcile the runtime to the documentation, in that direction. Add issue-command-show and issue-command-list to the xos allowlist in the ops runner, then refresh the ops branch from the default branch so the deployed runner actually carries the change - a runner change that is merged but not refreshed onto ops is invisible to this channel, which is plausibly how the gap arose in the first place. THE PROPERTIES THAT MUST NOT CHANGE. Both commands stay strictly READ-ONLY and METADATA-ONLY. They may return identity, action, actor, timing, outcome, the envelope hash, and the payload's recognised field NAMES plus a count of the rest - never the submitted payload values, and never an unrecognised key name, since both are author-controlled and this channel's results are public. They must remain unable to execute, re-execute, retry or amend anything: a committed receipt is terminal, and a reader that could act on one would turn an audit record into a control surface. issue-command-list keeps its 1-100 limit clamp. FIRST, CHECK WHICH SIDE IS WRONG. If either command is genuinely absent from the CLI rather than merely from the allowlist, the correct repair is the opposite one - amend both documents to describe what exists - because a runbook step that cannot be performed is worse than an honest gap. Verify the CLI before changing the allowlist. NOT IN SCOPE: any change to the issue-command transport, the ledger, the exactly-once semantics or the redaction rules. This is a read-path allowlist entry and nothing more.\",\"reason\":\"documented capability missing from the deployed runner; repair whichever side is actually wrong, and preserve the metadata-only guarantee either way\"}}"}, "id": "lo-t2-cmd3"}