{"type":"db","sql":"select mode, count(*) as runs, max(started_at)::text as latest, round(extract(epoch from (max(started_at)-min(started_at)))/nullif(count(*)-1,0))::text as avg_gap_secs from bot_runs where started_at > now() - interval '3 hours' group by 1 order by 2 desc","max_rows":10,"id":"heartbeat-cal"}
