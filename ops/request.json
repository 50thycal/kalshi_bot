{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-t2-ev-refusal-20260822\",\"action\":\"ADD_EVIDENCE\",\"actor\":\"claude-code\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"issue\":\"XOS-000005\",\"evidence_type\":\"OPS_RESULT\",\"summary\":\"THE REFUSAL, AND THE EXACT DEPLOYED ALLOWLIST. An xos request for issue-command-show, submitted immediately after a successfully executed TRIAGE envelope, was refused: the runner printed that the command is not allowlisted and enumerated the set it does accept - control-tower, enforcement, evaluate-gates, issue-candidates, issue-findings-plan, issue-list, issue-show, list, metric, platform, readiness, scoreboard, show, tag, transitions. Neither issue-command-show nor issue-command-list is present. That enumeration comes from the runner itself, so it is the authoritative statement of what production accepts, and it contradicts both documents that describe the receipt read as available. The mismatch is one-directional: the docs promise more than the runtime provides, which is the direction that strands an operator mid-procedure. The documented sequence in docs/EXPERIMENT_OS_ISSUES.md ('Doing it', steps 3 and 4) cannot be followed as written - step 3 is the refused read. WORKAROUND USED, and its limit: all nine envelopes driven this session were confirmed instead through issue-show on the target ticket, which proves the resulting STATE but not the OUTCOME of the attempt. A REJECTED envelope and one that never executed are indistinguishable that way, and the sanitized error a FAILED receipt carries is unreachable. Every write happened to succeed here, so the gap cost nothing this time; that is luck, not coverage.\",\"source_ref\":\"ops/results/lo-x3-rcpt1.txt\"}}"}, "id": "lo-t2-cmd2"}