{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-valplan-receiptvis-20260824\",\"action\":\"RECORD_VALIDATION_PLAN\",\"actor\":\"cal\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"issue\":\"XOS-000010\",\"validation_plan\":\"Three things must be shown, and the second is the one that can fail quietly. (1) OUTCOME COVERAGE. Exercise all four receipt outcomes - SUCCEEDED, REJECTED, FAILED and DEFERRED - and for each, read the line back THROUGH THE PRODUCTION OPS LOGS CHANNEL, not from a local logger or a structured sink. The operator must be able to read the status, the action, the command identifier or its safe hash prefix, the receipt identifier and, where applicable, the stable error code, from the message text alone. Reading it from log_event fields does not count: that is exactly what is already true and already invisible. (2) NO DISCLOSURE. Submit envelopes whose payload values and unknown key names carry distinctive MARKER strings, drive each of the four outcomes, and then prove that no marker appears anywhere in the logs - the operator-visible message, any structured field, and the boot hook's own output. Include a FAILED case whose exception would naturally quote the offending value and its bound parameters, since that is the path that regresses. Absence of the marker is the assertion; 'looks fine' is not. (3) NOTHING ELSE MOVED. The receipt ledger is unchanged in shape and content, and exactly-once still holds: a replayed command_id executes nothing and returns the stored receipt, and a same-id-different-envelope submission is still a collision. Tests are preferable to a one-time manual check for (1) and (2), for the reason XOS-000005 established: a surface that is only checked once drifts back.\",\"reason\":\"pre-registering what would count as proof before anyone builds the repair\"}}"}, "id": "lo7-valplan-set"}