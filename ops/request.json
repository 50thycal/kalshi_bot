{"type":"db","sql":"select strategy, status, count(*) as n, min(created_at) as first, max(created_at) as last from live_orders where created_at > now() - interval '2 hours' group by 1,2 order by 1,2","max_rows":20,"id":"db-live-ord"}
