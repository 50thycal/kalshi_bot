{"type": "env", "set": {"EXPERIMENT_OS_ISSUE_COMMAND": "{\"command_id\":\"lo-ev7-valpartial-20260824\",\"action\":\"ADD_EVIDENCE\",\"actor\":\"cal\",\"actor_role\":\"LIVE_OPS\",\"schema_version\":1,\"payload\":{\"issue\":\"XOS-000007\",\"evidence_type\":\"PR\",\"summary\":\"VALIDATION AGAINST THE PRE-REGISTERED PLAN, three of four checks. CHECK 1 - NORMAL TRAFFIC STILL WORKS: PASS on the pre-protection baseline. Ordinary request commits were pushed to ops throughout this work (ids lo7-show-1, lo7-isslist-1, lo7-issall-1, lo7-cand-1, plus the issue-command env sets and their receipt reads); every one fired the runner and every result commit landed at ops/results/<id>.txt. Both sides are fast-forward pushes, which the proposed ruleset does not restrict, so the check must be re-run after the ruleset is applied to be conclusive. CHECK 3 - THE INSTRUCTIONS NO LONGER POINT AT THE HAZARD: PASS. Every standing instruction to force-refresh ops is retired in CLAUDE.md, docs/OPS_RUNBOOK.md, the kalshi-probe-builder skill and its probe-and-ops reference, and two stale operational notes in docs/RESEARCH_JOURNAL.md and docs/IDEA_MODEL_20260711_run2.md. A separate instruction telling sessions to overlay a corrected script onto ops and run it from there was also removed - the runner's freshness guard refuses that path outright since XOS-000005. tests/test_ops_branch_protection.py asserts the absence per instruction file rather than relying on a one-time edit, and asserts the runbook's maintenance procedure names the idle-channel check, the backup branch, the expected-SHA lease, the post-change OPS_RUNNER_CODE_SOURCE check and the recovery path. CHECK 4 - THE WORKFLOW CANNOT SILENTLY REGRESS: PASS. A request served during this work ran the two-checkout workflow, the second checkout into .ops-runner-code at the default branch, with the request executed under OPS_RUNNER_CODE_SOURCE=default-branch. ops is transport only; no code is executed from it. CHECK 2 - AN UNAUTHORIZED FORCE UPDATE IS REJECTED: NOT YET RUN. It cannot be exercised until the ruleset is actually applied, which needs a repo-admin PAT in the OPS_ADMIN_TOKEN secret and the workflow merged to the default branch. Until then the branch remains unprotected and this ticket must not be resolved.\",\"source_ref\":\"https://github.com/50thycal/kalshi_bot/pull/260\"}}"}, "id": "lo7-ev2-set"}