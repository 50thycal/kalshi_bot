{"type":"db","id":"lo359-tape-1","max_rows":40,"sql":"select 'perp_market_snapshots' as t, count(*) as rows, max(observed_at) as latest, count(*) filter (where observed_at > now() - interval '24 hours') as last_24h from perp_market_snapshots union all select 'perp_collector_cycles', count(*), max(started_at), count(*) filter (where started_at > now() - interval '24 hours') from perp_collector_cycles union all select 'perp_orderbook_snapshots', count(*), max(observed_at), count(*) filter (where observed_at > now() - interval '24 hours') from perp_orderbook_snapshots"}
