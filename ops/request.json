{"type":"db","sql":"SELECT split_part(market_ticker,'-',1) AS family, status, coalesce(cancel_reason,'(null)') AS reason, count(*) AS n, min(created_at)::text AS first_seen, max(created_at)::text AS last_seen FROM live_orders GROUP BY 1,2,3 ORDER BY 1,4 DESC","max_rows":200,"id":"lo-tc-radius-1"}
