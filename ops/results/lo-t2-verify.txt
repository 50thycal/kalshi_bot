# ops request: type='xos'
{
  "issue_key": "XOS-000005",
  "title": "issue-command-show and issue-command-list are documented as ops-allowlisted but are absent from the deployed xos allowlist, so issue-command receipts are unreadable in production",
  "status": "OPEN",
  "classification": "OPS",
  "severity": "LOW",
  "priority": "P3",
  "owner_role": "LIVE_OPS",
  "opened_by_role": "LIVE_OPS",
  "opened_at": "2026-08-23 02:33:42.289107+00:00",
  "scope": "system-wide",
  "experiment_id": null,
  "version_id": null,
  "detector": "manual.reported",
  "detector_fingerprint": null,
  "occurrence_count": 1,
  "disposition": null,
  "latest_evidence": "OPS_RESULT: THE REFUSAL, AND THE EXACT DEPLOYED ALLOWLIST. An xos request for issue-command-",
  "problem_statement": "DOCUMENTATION/RUNTIME MISMATCH IN THE OPS CHANNEL. Both docs/EXPERIMENT_OS_ISSUES.md and docs/OPS_RUNBOOK.md document issue-command-show and issue-command-list as allowlisted xos commands, and the issue-write runbook names issue-command-show as the step that reads a submitted command's receipt before confirming the effect through the canonical read. The deployed ops runner does not carry them: an xos request for issue-command-show is refused with 'not allowlisted', and the refusal prints the accepted set - control-tower, enforcement, evaluate-gates, issue-candidates, issue-findings-plan, issue-list, issue-show, list, metric, platform, readiness, scoreboard, show, tag, transitions. Neither issue-command command appears. CONSEQUENCE: receipts are unreadable through the only channel that can reach production. Exactly-once is a property of the committed receipt, so an operator submitting an issue command through EXPERIMENT_OS_ISSUE_COMMAND cannot read what their envelope actually did - whether it SUCCEEDED, was REJECTED, or FAILED, or what the sanitized error said. They can only infer success from the canonical issue-show, which shows the resulting state but not the outcome of the attempt; a REJECTED envelope and one that was never executed look identical from there. This is a gap in operational readback, not a correctness problem: the transport, the ledger and the mutation path are unaffected, and no write is at risk.",
  "evidence_summary": "Opened from a Live Ops runtime observation while driving nine issue-command envelopes for XOS-000003 and XOS-000004 - the documented receipt read was refused every time and issue-show had to be used as a substitute. Outside the Control Tower candidate-detector surface, so OPEN_MANUAL is the correct door. Kept separate from XOS-000004 because the two share nothing but a reporter: this is a stale allowlist in the ops runner, that one is a defect in what the workers emit and what the logs channel returns.",
  "proposed_fix": "Reconcile the runtime to the documentation, in that direction. Add issue-command-show and issue-command-list to the xos allowlist in the ops runner, then refresh the ops branch from the default branch so the deployed runner actually carries the change - a runner change that is merged but not refreshed onto ops is invisible to this channel, which is plausibly how the gap arose in the first place. THE PROPERTIES THAT MUST NOT CHANGE. Both commands stay strictly READ-ONLY and METADATA-ONLY. They may return identity, action, actor, timing, outcome, the envelope hash, and the payload's recognised field NAMES plus a count of the rest - never the submitted payload values, and never an unrecognised key name, since both are author-controlled and this channel's results are public. They must remain unable to execute, re-execute, retry or amend anything: a committed receipt is terminal, and a reader that could act on one would turn an audit record into a control surface. issue-command-list keeps its 1-100 limit clamp. FIRST, CHECK WHICH SIDE IS WRONG. If either command is genuinely absent from the CLI rather than merely from the allowlist, the correct repair is the opposite one - amend both documents to describe what exists - because a runbook step that cannot be performed is worse than an honest gap. Verify the CLI before changing the allowlist. NOT IN SCOPE: any change to the issue-command transport, the ledger, the exactly-once semantics or the redaction rules. This is a read-path allowlist entry and nothing more.",
  "validation_plan": "Both halves must be shown: the commands become READABLE through the production ops channel, and they remain METADATA-ONLY. Five checks, all performed through the ops channel against production, each citing its ops result id as closing evidence. (1) READABLE: an xos issue-command-show for a known command_id returns a receipt instead of a not-allowlisted refusal. Use one of this session's real command ids - lo-x3-triage-20260822 is a SUCCEEDED TRIAGE on XOS-000003 - so the read is checked against an envelope whose effect is already independently visible. (2) LISTABLE: issue-command-list returns the recent receipts, and its limit clamps to the 1-100 range at both ends rather than erroring or being ignored. (3) OUTCOMES ARE DISTINGUISHABLE: a SUCCEEDED receipt and a REJECTED one read differently, and the REJECTED one carries its sanitized reason. This is the capability the ticket exists for - proving only that a receipt renders would not show that the gap is closed, since the point is telling a rejected envelope from one that never ran. (4) METADATA ONLY - the load-bearing negative check: the rendered receipt shows recognised payload field NAMES and a count of the rest, and shows NO submitted payload value and NO unrecognised key name. Confirm against an envelope with a long free-text payload; several of this session's carry multi-hundred-character summaries, so leakage would be obvious. (5) STILL INERT: reading a receipt executes, retries and changes nothing - the target issue's state and event count are identical before and after, and no new receipt is written by the read. If check (4) or (5) fails the repair must be reverted rather than resolved, because a leaking or acting reader is worse than the current gap: today the capability is merely missing, and a public channel that echoes author-controlled payload text is a disclosure.",
  "resolution_summary": null,
  "resolved_at": null,
  "requires_new_version": null,
  "requires_new_epoch": null,
  "requires_platform_revision": null,
  "requires_pause_or_stand_down": null,
  "duplicate_of_issue_id": null,
  "supersedes_issue_id": null,
  "discovered_from_issue_id": null,
  "first_observed_at": "2026-08-23 02:33:42.289107+00:00",
  "last_observed_at": "2026-08-23 02:33:42.289107+00:00",
  "details_json": {
    "discovery": "Opened from a Live Ops runtime observation while driving nine issue-command envelopes for XOS-000003 and XOS-000004 - the documented receipt read was refused every time and issue-show had to be used as a substitute. Outside the Control Tower candidate-detector surface, so OPEN_MANUAL is the correct door. Kept separate from XOS-000004 because the two share nothing but a reporter: this is a stale allowlist in the ops runner, that one is a defect in what the workers emit and what the logs channel returns.",
    "reported_via": "issue_command_transport",
    "manual_report": true
  },
  "events": [
    {
      "event_type": "CREATED",
      "occurred_at": "2026-08-23 02:33:42.289107+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": "OPEN",
      "from_owner_role": null,
      "to_owner_role": "LIVE_OPS",
      "from_classification": null,
      "to_classification": "OPS",
      "reason": "Opened from a Live Ops runtime observation while driving nine issue-command envelopes for XOS-000003 and XOS-000004 - the documented receipt read was refused every time and issue-show had to be used as a substitute. Outside the Control Tower candidate-detector surface, so OPEN_MANUAL is the correct door. Kept separate from XOS-000004 because the two share nothing but a reporter: this is a stale allowlist in the ops runner, that one is a defect in what the workers emit and what the logs channel returns.",
      "payload": {
        "scope": "system-wide",
        "anomaly_kind": null,
        "recommended_owner": {
          "priority": "P2",
          "severity": "MEDIUM",
          "rationale": "insufficient evidence to classify \u2014 route to Live Ops for operational diagnosis first, or leave with the operator to triage. The owner here is who looks first, NOT a claim that the cause is operational",
          "owner_role": "LIVE_OPS",
          "disposition": null,
          "classification": "UNCLASSIFIED",
          "requires_operator_triage": true
        }
      }
    },
    {
      "event_type": "EVIDENCE_ADDED",
      "occurred_at": "2026-08-23 02:37:29.992038+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": null,
      "from_owner_role": null,
      "to_owner_role": null,
      "from_classification": null,
      "to_classification": null,
      "reason": "THE REFUSAL, AND THE EXACT DEPLOYED ALLOWLIST. An xos request for issue-command-show, submitted immediately after a successfully executed TRIAGE envelope, was refused: the runner printed that the command is not allowlisted and enumerated the set it does accept - control-tower, enforcement, evaluate-gates, issue-candidates, issue-findings-plan, issue-list, issue-show, list, metric, platform, readiness, scoreboard, show, tag, transitions. Neither issue-command-show nor issue-command-list is presen",
      "payload": {
        "source_ref": "ops/results/lo-x3-rcpt1.txt",
        "evidence_id": 16,
        "content_hash": null,
        "evidence_type": "OPS_RESULT"
      }
    },
    {
      "event_type": "FIX_PROPOSED",
      "occurred_at": "2026-08-23 02:39:42.320874+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": null,
      "from_owner_role": null,
      "to_owner_role": null,
      "from_classification": null,
      "to_classification": null,
      "reason": "documented capability missing from the deployed runner; repair whichever side is actually wrong, and preserve the metadata-only guarantee either way",
      "payload": {
        "proposed_fix": "Reconcile the runtime to the documentation, in that direction. Add issue-command-show and issue-command-list to the xos allowlist in the ops runner, then refresh the ops branch from the default branch so the deployed runner actually carries the change - a runner change that is merged but not refreshed onto ops is invisible to this channel, which is plausibly how the gap arose in the first place. THE PROPERTIES THAT MUST NOT CHANGE. Both commands stay strictly READ-ONLY and METADATA-ONLY. They may return identity, action, actor, timing, outcome, the envelope hash, and the payload's recognised field NAMES plus a count of the rest - never the submitted payload values, and never an unrecognised key name, since both are author-controlled and this channel's results are public. They must remain unable to execute, re-execute, retry or amend anything: a committed receipt is terminal, and a reader that could act on one would turn an audit record into a control surface. issue-command-list keeps its 1-100 limit clamp. FIRST, CHECK WHICH SIDE IS WRONG. If either command is genuinely absent from the CLI rather than merely from the allowlist, the correct repair is the opposite one - amend both documents to describe what exists - because a runbook step that cannot be performed is worse than an honest gap. Verify the CLI before changing the allowlist. NOT IN SCOPE: any change to the issue-command transport, the ledger, the exactly-once semantics or the redaction rules. This is a read-path allowlist entry and nothing more."
      }
    },
    {
      "event_type": "VALIDATION_PLANNED",
      "occurred_at": "2026-08-23 02:42:06.318715+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": null,
      "from_owner_role": null,
      "to_owner_role": null,
      "from_classification": null,
      "to_classification": null,
      "reason": "pre-registering that readability alone is not the bar - metadata-only and inert must be demonstrated too",
      "payload": {
        "validation_plan": "Both halves must be shown: the commands become READABLE through the production ops channel, and they remain METADATA-ONLY. Five checks, all performed through the ops channel against production, each citing its ops result id as closing evidence. (1) READABLE: an xos issue-command-show for a known command_id returns a receipt instead of a not-allowlisted refusal. Use one of this session's real command ids - lo-x3-triage-20260822 is a SUCCEEDED TRIAGE on XOS-000003 - so the read is checked against an envelope whose effect is already independently visible. (2) LISTABLE: issue-command-list returns the recent receipts, and its limit clamps to the 1-100 range at both ends rather than erroring or being ignored. (3) OUTCOMES ARE DISTINGUISHABLE: a SUCCEEDED receipt and a REJECTED one read differently, and the REJECTED one carries its sanitized reason. This is the capability the ticket exists for - proving only that a receipt renders would not show that the gap is closed, since the point is telling a rejected envelope from one that never ran. (4) METADATA ONLY - the load-bearing negative check: the rendered receipt shows recognised payload field NAMES and a count of the rest, and shows NO submitted payload value and NO unrecognised key name. Confirm against an envelope with a long free-text payload; several of this session's carry multi-hundred-character summaries, so leakage would be obvious. (5) STILL INERT: reading a receipt executes, retries and changes nothing - the target issue's state and event count are identical before and after, and no new receipt is written by the read. If check (4) or (5) fails the repair must be reverted rather than resolved, because a leaking or acting reader is worse than the current gap: today the capability is merely missing, and a public channel that echoes author-controlled payload text is a disclosure."
      }
    }
  ],
  "evidence": [
    {
      "evidence_type": "OPS_RESULT",
      "summary": "THE REFUSAL, AND THE EXACT DEPLOYED ALLOWLIST. An xos request for issue-command-show, submitted immediately after a successfully executed TRIAGE envelope, was refused: the runner printed that the command is not allowlisted and enumerated the set it does accept - control-tower, enforcement, evaluate-gates, issue-candidates, issue-findings-plan, issue-list, issue-show, list, metric, platform, readiness, scoreboard, show, tag, transitions. Neither issue-command-show nor issue-command-list is present. That enumeration comes from the runner itself, so it is the authoritative statement of what production accepts, and it contradicts both documents that describe the receipt read as available. The mismatch is one-directional: the docs promise more than the runtime provides, which is the direction that strands an operator mid-procedure. The documented sequence in docs/EXPERIMENT_OS_ISSUES.md ('Doing it', steps 3 and 4) cannot be followed as written - step 3 is the refused read. WORKAROUND USED, and its limit: all nine envelopes driven this session were confirmed instead through issue-show on the target ticket, which proves the resulting STATE but not the OUTCOME of the attempt. A REJECTED envelope and one that never executed are indistinguishable that way, and the sanitized error a FAILED receipt carries is unreachable. Every write happened to succeed here, so the gap cost nothing this time; that is luck, not coverage.",
      "source_ref": "ops/results/lo-x3-rcpt1.txt",
      "captured_at": "2026-08-23 02:37:29.987118+00:00",
      "captured_by": "claude-code",
      "content_hash": null
    }
  ],
  "links": []
}
