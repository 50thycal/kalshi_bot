# ops request: type='xos'
{
  "issue_key": "XOS-000004",
  "title": "Series-addressed books can see zero markets indefinitely with no operator-visible warning, and their funnel counters are unreadable in production",
  "status": "OPEN",
  "classification": "OPS",
  "severity": "MEDIUM",
  "priority": "P2",
  "owner_role": "LIVE_OPS",
  "opened_by_role": "LIVE_OPS",
  "opened_at": "2026-08-23 02:21:18.661376+00:00",
  "scope": "system-wide",
  "experiment_id": null,
  "version_id": null,
  "detector": "manual.reported",
  "detector_fingerprint": null,
  "occurrence_count": 1,
  "disposition": null,
  "latest_evidence": "OPS_RESULT: THE COUNTERS EXIST BUT ARE UNREACHABLE FROM PRODUCTION-VISIBLE OUTPUT. The book ",
  "problem_statement": "CROSS-BOOK OPERATIONAL OBSERVABILITY DEFECT. Any book that addresses its universe by a configured list of Kalshi series tickers can fetch zero open markets on every cycle, forever, and produce output indistinguishable from a healthy book. Two independent gaps combine. (1) NO EMPTY-SERIES WARNING: the fetch loop calls get_markets(status=open, series_ticker=S) per configured series; a series that matches nothing on the venue returns HTTP 200 with an empty list, which is not an exception, so the existing except-and-warn path never fires and nothing is logged. A mistyped, renamed, delisted or never-validated series ticker is therefore silent by construction. (2) FUNNEL COUNTERS UNREADABLE: the per-cycle counters that would localise the failure (markets seen, classified, eligible, pinned, opened, skipped_illiquid, skipped_discount, skipped_price) are emitted as structured log ATTRIBUTES via log_event/JsonFormatter, but the ops logs channel queries only timestamp, message and severity, so an operator sees the bare message text and none of the numbers. Combined effect: an operator cannot tell a book that is finding nothing from a book that is finding candidates and rejecting them, and cannot identify which funnel stage first reaches zero, from production-visible output alone. This is a diagnostic-capability defect in shared operational tooling, not a property of any one strategy.",
  "evidence_summary": "Opened from a Live Ops runtime observation during the XOS-000003 diagnosis. It is deliberately NOT part of that ticket: XOS-000003 is a strategy conclusion about one experiment and has been transferred to Research Lab, whereas this defect affects every series-addressed execution path and is repairable without touching any experiment contract. It is also outside the Control Tower candidate-detector surface (issue-candidates lists only the two live-canary gate blockers), so OPEN_MANUAL rather than OPEN_CANDIDATE is the correct door.",
  "proposed_fix": "Close both gaps at the layer they belong to; neither touches a strategy rule, a universe definition or any experiment contract. (1) WARN ON AN EMPTY SERIES. In the shared series-addressed fetch path, count the open markets returned per configured series and emit a WARNING naming every series that returned zero for the whole cycle - message text, not an attribute, so it survives the ops read path. Rate-limit it to once per series per cycle so a legitimately quiet series does not flood the log, and make the message state the count and the series so a mistyped, renamed or delisted ticker is self-identifying. Zero markets from EVERY configured series should be a distinct, louder line, because that is the condition that makes a book inert. (2) MAKE THE FUNNEL READABLE IN PRODUCTION. Either fold the funnel counters into the log MESSAGE for the books that have them, or add attributes to the deploymentLogs GraphQL selection in the logs script so structured fields survive to the operator. The second is the better fix - it is one query change, it recovers every structured field the workers already emit rather than just this book's, and it makes attribute filtering behave as the runbook implies. The first is the cheap fallback if the Railway schema will not return attributes. NOT IN SCOPE: correcting any particular book's configured series list. That is a per-book question and, for FREEZE specifically, is already answered on XOS-000003 and owned by Research Lab. This ticket is about being able to SEE the condition, not about any one book's universe being right.",
  "validation_plan": "The bar is a CAPABILITY demonstration, not a code review: an operator with only the ops channel must be able to name the first funnel stage that reaches zero, for a book they have never looked at, without reading source and without a database query. Four checks, each performed against PRODUCTION output after the repair deploys. (1) EMPTY-SERIES WARNING FIRES: with the current configuration unchanged, a logs read shows a warning naming the configured series that returned zero open markets on the most recent cycle. FREEZE is the natural subject because all seven of its series are known-empty, so a correct implementation must warn on all seven; if it warns on none, the repair has not landed. (2) FUNNEL IS READABLE: the same logs read shows the per-cycle counters (markets, commodity, eligible, pinned, opened, and each skip reason) as values an operator can read, not as a bare message. (3) THE DISCRIMINATION ACTUALLY WORKS: for one book seeing zero markets and one book seeing markets and rejecting them, production output alone distinguishes the two cases and localises the first zero to a named stage. The second half matters as much as the first - a repair that only proves the failing case does not prove the capability. (4) NO REGRESSION IN SIGNAL-TO-NOISE: a normally-quiet series does not produce a warning on every cycle, and the trading cycle's log volume is not materially increased. Closing evidence must cite the ops result ids for the reads above. If check (3) cannot be demonstrated, the ticket returns to INVESTIGATING rather than resolving - the observed symptom being gone is not the same as the diagnosis being possible next time.",
  "resolution_summary": null,
  "resolved_at": null,
  "requires_new_version": null,
  "requires_new_epoch": null,
  "requires_platform_revision": null,
  "requires_pause_or_stand_down": null,
  "duplicate_of_issue_id": null,
  "supersedes_issue_id": null,
  "discovered_from_issue_id": null,
  "first_observed_at": "2026-08-23 02:21:18.661376+00:00",
  "last_observed_at": "2026-08-23 02:21:18.661376+00:00",
  "details_json": {
    "discovery": "Opened from a Live Ops runtime observation during the XOS-000003 diagnosis. It is deliberately NOT part of that ticket: XOS-000003 is a strategy conclusion about one experiment and has been transferred to Research Lab, whereas this defect affects every series-addressed execution path and is repairable without touching any experiment contract. It is also outside the Control Tower candidate-detector surface (issue-candidates lists only the two live-canary gate blockers), so OPEN_MANUAL rather than OPEN_CANDIDATE is the correct door.",
    "reported_via": "issue_command_transport",
    "manual_report": true
  },
  "events": [
    {
      "event_type": "CREATED",
      "occurred_at": "2026-08-23 02:21:18.661376+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": "OPEN",
      "from_owner_role": null,
      "to_owner_role": "LIVE_OPS",
      "from_classification": null,
      "to_classification": "OPS",
      "reason": "Opened from a Live Ops runtime observation during the XOS-000003 diagnosis. It is deliberately NOT part of that ticket: XOS-000003 is a strategy conclusion about one experiment and has been transferred to Research Lab, whereas this defect affects every series-addressed execution path and is repairable without touching any experiment contract. It is also outside the Control Tower candidate-detector surface (issue-candidates lists only the two live-canary gate blockers), so OPEN_MANUAL rather than OPEN_CANDIDATE is the correct door.",
      "payload": {
        "scope": "system-wide",
        "anomaly_kind": null,
        "recommended_owner": {
          "priority": "P2",
          "severity": "MEDIUM",
          "rationale": "insufficient evidence to classify \u2014 route to Live Ops for operational diagnosis first, or leave with the operator to triage. The owner here is who looks first, NOT a claim that the cause is operational",
          "owner_role": "LIVE_OPS",
          "disposition": null,
          "classification": "UNCLASSIFIED",
          "requires_operator_triage": true
        }
      }
    },
    {
      "event_type": "EVIDENCE_ADDED",
      "occurred_at": "2026-08-23 02:24:22.453516+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": null,
      "from_owner_role": null,
      "to_owner_role": null,
      "from_classification": null,
      "to_classification": null,
      "reason": "OBSERVED INSTANCE - nine days of healthy-looking output over a universe of zero. The FREEZE paper book (freeze1-4) logged its cycle every ~15 minutes across the whole window sampled (15:50Z to 20:53Z on 2026-08-22), on a worker whose latest deployment was SUCCESS, and the cycle line is indistinguishable from a productive one. Across that window there is no 'markets fetch failed' warning of any kind, because the seven configured series return HTTP 200 with an empty list rather than raising - the ",
      "payload": {
        "source_ref": "ops/results/lo-x3-logs-1.txt",
        "evidence_id": 14,
        "content_hash": null,
        "evidence_type": "LOG"
      }
    },
    {
      "event_type": "EVIDENCE_ADDED",
      "occurred_at": "2026-08-23 02:26:42.370288+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": null,
      "from_owner_role": null,
      "to_owner_role": null,
      "from_classification": null,
      "to_classification": null,
      "reason": "THE COUNTERS EXIST BUT ARE UNREACHABLE FROM PRODUCTION-VISIBLE OUTPUT. The book already computes exactly the funnel a diagnosis needs and passes it to log_event as keyword fields; JsonFormatter merges them into the emitted JSON line. They do not survive the read path: the ops logs channel's GraphQL query requests only timestamp, message and severity from deploymentLogs, so every structured field is dropped before an operator ever sees it, and the result is the bare string 'freeze book'. Attribut",
      "payload": {
        "source_ref": "ops/results/lo-x3-logs-attr.txt",
        "evidence_id": 15,
        "content_hash": null,
        "evidence_type": "OPS_RESULT"
      }
    },
    {
      "event_type": "FIX_PROPOSED",
      "occurred_at": "2026-08-23 02:28:58.755793+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": null,
      "from_owner_role": null,
      "to_owner_role": null,
      "from_classification": null,
      "to_classification": null,
      "reason": "the repair is observability, deliberately separated from any experiment's universe question",
      "payload": {
        "proposed_fix": "Close both gaps at the layer they belong to; neither touches a strategy rule, a universe definition or any experiment contract. (1) WARN ON AN EMPTY SERIES. In the shared series-addressed fetch path, count the open markets returned per configured series and emit a WARNING naming every series that returned zero for the whole cycle - message text, not an attribute, so it survives the ops read path. Rate-limit it to once per series per cycle so a legitimately quiet series does not flood the log, and make the message state the count and the series so a mistyped, renamed or delisted ticker is self-identifying. Zero markets from EVERY configured series should be a distinct, louder line, because that is the condition that makes a book inert. (2) MAKE THE FUNNEL READABLE IN PRODUCTION. Either fold the funnel counters into the log MESSAGE for the books that have them, or add attributes to the deploymentLogs GraphQL selection in the logs script so structured fields survive to the operator. The second is the better fix - it is one query change, it recovers every structured field the workers already emit rather than just this book's, and it makes attribute filtering behave as the runbook implies. The first is the cheap fallback if the Railway schema will not return attributes. NOT IN SCOPE: correcting any particular book's configured series list. That is a per-book question and, for FREEZE specifically, is already answered on XOS-000003 and owned by Research Lab. This ticket is about being able to SEE the condition, not about any one book's universe being right."
      }
    },
    {
      "event_type": "VALIDATION_PLANNED",
      "occurred_at": "2026-08-23 02:31:18.092591+00:00",
      "actor": "claude-code",
      "actor_role": "LIVE_OPS",
      "from_status": null,
      "to_status": null,
      "from_owner_role": null,
      "to_owner_role": null,
      "from_classification": null,
      "to_classification": null,
      "reason": "pre-registering what would count as proof, before anyone builds the repair",
      "payload": {
        "validation_plan": "The bar is a CAPABILITY demonstration, not a code review: an operator with only the ops channel must be able to name the first funnel stage that reaches zero, for a book they have never looked at, without reading source and without a database query. Four checks, each performed against PRODUCTION output after the repair deploys. (1) EMPTY-SERIES WARNING FIRES: with the current configuration unchanged, a logs read shows a warning naming the configured series that returned zero open markets on the most recent cycle. FREEZE is the natural subject because all seven of its series are known-empty, so a correct implementation must warn on all seven; if it warns on none, the repair has not landed. (2) FUNNEL IS READABLE: the same logs read shows the per-cycle counters (markets, commodity, eligible, pinned, opened, and each skip reason) as values an operator can read, not as a bare message. (3) THE DISCRIMINATION ACTUALLY WORKS: for one book seeing zero markets and one book seeing markets and rejecting them, production output alone distinguishes the two cases and localises the first zero to a named stage. The second half matters as much as the first - a repair that only proves the failing case does not prove the capability. (4) NO REGRESSION IN SIGNAL-TO-NOISE: a normally-quiet series does not produce a warning on every cycle, and the trading cycle's log volume is not materially increased. Closing evidence must cite the ops result ids for the reads above. If check (3) cannot be demonstrated, the ticket returns to INVESTIGATING rather than resolving - the observed symptom being gone is not the same as the diagnosis being possible next time."
      }
    }
  ],
  "evidence": [
    {
      "evidence_type": "LOG",
      "summary": "OBSERVED INSTANCE - nine days of healthy-looking output over a universe of zero. The FREEZE paper book (freeze1-4) logged its cycle every ~15 minutes across the whole window sampled (15:50Z to 20:53Z on 2026-08-22), on a worker whose latest deployment was SUCCESS, and the cycle line is indistinguishable from a productive one. Across that window there is no 'markets fetch failed' warning of any kind, because the seven configured series return HTTP 200 with an empty list rather than raising - the fetch loop's except-and-warn path is never reached. Nothing in the log says which series were queried, that any of them matched nothing, or how many markets were seen. The book had in fact placed zero orders since 2026-08-15 (its epoch floor) and the operator-visible output never changed shape at any point. Two earlier silent failures on the same book are relevant precedent for how expensive this blindness is: the promotion wired the tracker into _run_live_cycle without ever calling it (fixed 2026-08-15, commit 6aa46e0, found only by a manual database query), and this zero-universe condition then persisted undetected until the Control Tower's zero-evidence detector fired - a detector that reports the SYMPTOM and by design cannot localise the stage.",
      "source_ref": "ops/results/lo-x3-logs-1.txt",
      "captured_at": "2026-08-23 02:24:22.446889+00:00",
      "captured_by": "claude-code",
      "content_hash": null
    },
    {
      "evidence_type": "OPS_RESULT",
      "summary": "THE COUNTERS EXIST BUT ARE UNREACHABLE FROM PRODUCTION-VISIBLE OUTPUT. The book already computes exactly the funnel a diagnosis needs and passes it to log_event as keyword fields; JsonFormatter merges them into the emitted JSON line. They do not survive the read path: the ops logs channel's GraphQL query requests only timestamp, message and severity from deploymentLogs, so every structured field is dropped before an operator ever sees it, and the result is the bare string 'freeze book'. Attribute-level filtering is not a workaround either - a logs request filtered on an attribute predicate came back with the entire unfiltered stream (weather market, weather cycle, live cycle, polymarket index and so on), i.e. the predicate was ignored rather than applied. AFFECTED PATH, stated generally so this is not read as one book's problem: every book whose universe is addressed by a configured series list and fetched with get_markets(series_ticker=...) shares both gaps. FREEZE is the observed instance, not the scope. Any book using this addressing style, and any future one, fails the same way and produces the same undiagnosable output.",
      "source_ref": "ops/results/lo-x3-logs-attr.txt",
      "captured_at": "2026-08-23 02:26:42.365436+00:00",
      "captured_by": "claude-code",
      "content_hash": null
    }
  ],
  "links": []
}
