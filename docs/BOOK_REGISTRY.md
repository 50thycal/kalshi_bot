# Book registry — every trading book the worker runs

> **HISTORICAL RESEARCH DOCUMENTATION — not a lifecycle database.**
> Since the 2026-08-16 `NEW_ONLY` cutover, Experiment OS owns lifecycle state,
> arms, epochs, gates and evidence. The rows below are preserved for their thesis
> pointers, verdicts and measured history, which remain valuable. Do **not** read
> current state here — run `{"type":"xos","command":"control-tower"}`. Where a row
> disagrees with Experiment OS, Experiment OS is right.
>
> The "UNTRACKED book" reconciliation described below is obsolete by
> construction: under `NEW_ONLY` an unregistered tag cannot write a row at all.


**Canonical index of every book that writes `paper_trades` (or trades live).** The strategy
status loop reconciles the live `paper_trades.strategy` rows against this table every run: a
`book:<tag>` the query finds with **no row here is UNTRACKED** — the loop flags it (a book must
never trade without its rationale + pre-registered gate written down where the loop can see it).

**Who maintains this:** whoever builds or changes a book. The `kalshi-strategy` skill's Phase 5
(paper trade) REQUIRES adding a row here the moment a new book starts writing `paper_trades`, and
Phase 6 (live) REQUIRES updating its `status`. This file lives on the default branch (unlike the
loop's `STRATEGY_LOOP_STATUS.md`, which lives on `strategy-loop-status` and is loop-owned), so a
build/fable session can update it in the same PR that adds the book. Newest books at the top.

The `tag` column MUST match `paper_trades.strategy` exactly (or its prefix for a variant family),
so the loop can join on it. Keep the gate one line; the linked thesis doc carries the full detail.

---

## FEE RE-BASELINE — 2026-08-11. Read this before applying ANY gate below.

The paper engine billed resting maker entries at Kalshi's **taker** rate, ceiled to a whole cent
per order: **1.000¢/contract** against the **0.003¢** Kalshi actually charges a maker (measured,
n=342 live fills). The engine was corrected on **2026-08-11** (`kalshi_fee(..., maker=True)`).

**Size of the correction: +0.85 to +0.89¢/contract** in the 5–10¢ yes band (+0.89¢ at a 93¢ NO
entry). Every maker-book number is that much better than it used to read — the trade did not
change, only the bookkeeping.

**The boundary is a date, not a flag.** Maker-book history now spans two fee models, split at the
2026-08-11 deploy. A gate read across it must restrict to post-boundary trades or add the
difference back. The three live pairs got fresh `_pt3` twin epochs at the same deploy so the
comparisons that decide promotion sit entirely on one model.

**`mmsell_fill_model` handles this for you.** It normalizes every P&L to one fee model — adding
back the fee actually charged (stored per row) and re-applying the maker fee — so its `real_$/ct`
column and its EDGE/MIRAGE labels are already fee-model-independent and need no manual
adjustment. The `> +0.87¢` re-reads below are for gates applied to figures computed ANYWHERE ELSE
(a hand-written query, an older report, a number quoted in a doc before this date).

**What the correction does NOT rescue.** It is uniform across maker books, so it never reverses a
*relative* verdict. A book losing to its control still loses to it; the 2026-08-09 finding that
the wide band is dead (control `mmsell` at −1.80¢/trade, ≈−0.93¢ corrected) survives intact. The
correction moves the whole maker cohort up together — it does not re-rank it.

### Which gates moved, and which did not

| gate shape | fee-sensitive? | why |
|---|---|---|
| **Relative** — "beat control by ≥X¢", "Δmean ≥ −0.3¢", "beat `mmsell10a` by ≥0.5¢" | **NO** | both sides are maker books at the same clip, so the fee shifts them equally and cancels in the difference. The A1–A4 and offset-A/B gates are purely this shape and stand **unchanged**; the W/T gates contain a relative clause of this shape *plus* absolute clauses (next rows) that do move. |
| **Absolute paper** — theta4's "per-trade > 0", and the W/T families' "own ¢/trade > 0 absolute" (added 2026-08-09) | **YES** | now ~0.87¢ easier. Re-read as **> +0.87¢** on pre-boundary trades to preserve the bar that was actually intended. |
| **Realizable** — "realizable ¢/trade > 0" | **YES** | `mmsell_fill_model` computes realizable as *paper* P&L over the tickers live actually filled, so it carries the paper fee. Re-read as **> +0.87¢** on pre-boundary data. (The script itself now normalizes this — see below.) |
| **Break-even win rate** — A5's 93.9% | **YES** | the fee sits inside the break-even: p = (100 − entry + fee)/100. At a 7¢ entry, 93.9% was computed with the ~1¢ taker fee; with the maker fee it is **93.1%**. A5's n≥82 was sized against the old bar and is now conservative. |
| **Weather books** | **NO** | they enter at the ask — genuine takers, formula unchanged. Their −5.0¢/trade is real. |

### The MIRAGE verdicts must be re-read

Five books were labelled MIRAGE on a realizable figure between −0.24¢ and −0.85¢. Adding back the
0.87¢ the simulator was over-charging puts **all five at or slightly above zero** — they are
marginal, not negative:

| book | realizable as measured | + fee correction |
|---|---|---|
| `mmsell3` | −0.85¢ | ≈ +0.02¢ |
| `mmsell11` | −0.83¢ | ≈ +0.04¢ |
| `mmsell4` | −0.82¢ | ≈ +0.05¢ |
| `mmsell7` | −0.28¢ | ≈ +0.59¢ |
| `mmsell6` | −0.24¢ | ≈ +0.63¢ |

**This is a validation of the fix, not a rescue of the books.** Live `mmsell3` realized
**+0.18¢/trade** over n=359 before it was closed (`docs/MMSELL_LIVE_POSTMORTEM.md`) — the
corrected realizable estimate of ≈+0.02¢ lands on that live ground truth, where the −0.85¢
reading contradicted it by a full cent. The postmortem's conclusion is unchanged: `mmsell3` is
**breakeven, not profitable**, and breakeven is still not worth live capital. What changes is that
"MIRAGE" overstated the case — the correct verdict is "indistinguishable from zero".

## UNIVERSE BOUNDARIES — 2026-08-13 18:09:40Z, the market-type taxonomy

A second kind of dated boundary, and it must not be confused with the fee one above. **A fee
boundary is a uniform shift and is repaired by adjusting the bar. A universe boundary changes
WHICH MARKETS a book was offered, and is repaired only by discarding the earlier trades.** There
is no offset that converts a number computed over half a universe into one computed over all of
it.

| | 2026-08-11 fee | 2026-08-13 taxonomy |
|---|---|---|
| what moved | the bookkeeping on every maker trade, by the same ~0.87¢ | the *candidate set* of books selecting via `mtype=`/`mode=`/`xmtype=` |
| who is affected | every maker book | `Tmmsell1`, `Tmmsell2`, `Tmmsell5`, `Tmmsell6` only |
| remedy | add the shift back — pre-boundary data stays usable | **drop pre-boundary trades — no conversion exists** |
| relative gates | unaffected (both sides shift together) | **affected** — only the book's universe changed, not its control's |
| enforced by | `mmsell_fill_model`'s P&L normalization | `mmsell_fill_model`'s `COHORT_START` + `COHORT GATE READ` |

**Why the tags did not change.** The taxonomy change was purely additive — 46 series classified,
none reclassified — so every book's selection *rule* is byte-identical either side, and every
pre-boundary trade was a genuine instance of that rule. The books were simply offered ~half their
intended universe (an unclassified series is admitted by no allowlist), and specifically the half
the census prior was fit on. Forking the tags would assert a change that did not happen. **The
cohort is the date**, exactly as with the fee boundary — the difference is only in the remedy.

**What enforces it.** `COHORT_START` in `scripts/mmsell_fill_model.py` floors each affected book,
and the read prints a `COHORT GATE READ` section computing each against `mmsell10` **restricted to
the same window** — the right-hand side the relative gate always specified and which nothing
previously computed (the control's row in the main table is its lifetime). Pinned by
`tests/test_mmsell_fill_model.py`. This is the first boundary in this file that lives in code
rather than only in prose, because the standing read pools each tag's whole lifetime by default:
a boundary recorded only here would have been blended away on the next `mm check 1`.

**Consequence to state plainly when reporting these books:** all four gates restart at n=0 on
2026-08-13. `Tmmsell2` (~1 entry/hr) is the one that really hurts. In exchange, the 46 newly
classified series were never in the census, so post-boundary flow is the first genuinely
**out-of-sample** test the type hypothesis has had. Full detail: `docs/MMSELL_TYPE_BOOKS.md`.

| tag | status | thesis / rationale | one-line edge | pre-registered gate / kill criteria |
|---|---|---|---|---|
| `freeze1`–`freeze4` | **paper only** (built 2026-08-13) | `docs/FREEZE_THESIS.md` | Exchange-closure pin on the commodity hub: CBOT grains / ICE softs go dark while Kalshi trades 24/7, so a contract whose remaining window sits entirely inside a dark stretch is mechanically decided while retail still prices it cents from certainty. TAKER, single leg, hold to settlement. Promoted after `scripts/kalshi_freeze_study.py` cleared **all five** pre-registered gates 2026-08-12 (P1 +16.10¢/ct on 39,429 post-pin trades; P2 +15.50¢ over the favorite control; P3 the FREEZE cell itself; P4 ~$1.6M/wk notional; P5 zero wrong pins). Arms: **`freeze1`** dark window, ≥3¢ discount (the thesis) · **`freeze2`** dark, ≥8¢ (deep-discount slice) · **`freeze3`** **OPEN window, same bar — THE CONTROL** · **`freeze4`** dark, entry capped at 95¢. | **Gate on `freeze1` MINUS `freeze3`, never on `freeze1` alone.** The live book cannot see the settled result the backtest scored on, so it infers the decided side from the market's own favorite — which makes every arm a favorite-buy, the shape `tfav` died in (−3.6¢/trade, n=210). The claim is that favorites are underpriced *specifically while the source is dark*, so the control is the whole experiment. At **n≥150 settled per arm**: KEEP `freeze1` only if it beats `freeze3` by **≥+3¢/trade** (the thesis's own P1 bar) AND is **> 0 absolute**. KILL the family if `freeze1` ≤ `freeze3` — that is tfav on a new venue, and it closes the off-weather pinning family for good. `freeze2`/`freeze4` are read only against `freeze1` to size the discount/price bands; they cannot promote on their own. **Do not read the backtest as the verdict:** its +16.10¢ sits suspiciously near the +15.82¢ a v1 lookahead bug manufactured before it was caught, and 100% of it is one commodity (corn) that went 0→233 settled markets in a week. A forward paper test cannot have lookahead by construction, which is the main reason this book exists. **[2026-08-15] OUTAGE:** `_run_live_cycle` accepted `freeze_tracker` as a parameter but never called `_run_freeze_book` — a copy/paste gap from the same commit that promoted this book. Production runs `BOT_MODE=live`, so all four arms placed **zero** trades from promotion (2026-08-13 22:37 UTC) until the fix (2026-08-15). n≥150/arm now clocks from the fix, not the promotion date. `tests/test_live_cycle_ride_alongs.py` pins every ride-along tracker `_run_live_cycle` accepts against an actual call, so this class of bug fails CI instead of trading zero positions silently. |
| `mmsell10d`, `mmsell10e` | **RETIRED 2026-08-14** (scan-depth experiment, built 2026-08-13) — **MEASURED AND FAILED** | `docs/MMSELL_SCAN_DEPTH.md` | `mmsell10`'s exact entry (`lo=5,hi=10,maxyes=7`) allowed to look **225 events deep** into the volume-ranked list instead of the global 150 — the only difference, asserted by test. Answers whether the maker edge in the thinner tail of the board matches the top: ~1,740 eligible events/cycle are currently discarded by the cut without ever being priced, and every mmsell number we have comes from the top 150. | **`mmsell10` is the control** — same band, same ceiling, same window, read over the SAME window. At **n≥200 settled**, KEEP only if `mmsell10d` ¢/trade is **> 0 absolute** AND within **−0.5¢** of `mmsell10`. The bar is deliberately asymmetric: this book takes strictly MORE trades than its control (every event the control sees, plus 75), so it need not beat it — equal per-trade across more trades is more total P&L. KILL at ≤ 0 absolute, or >1.0¢ below control at n≥200, which would say the thin tail is worse and the cap was doing useful work. PROMOTE to a raised GLOBAL cap only on KEEP **and** no material rise in `RATE LIMITED` — buying trades by eating 429 backoffs steals from the rest of the cycle, live order management included. Affordable only because the Kalshi ADVANCED grant (2026-08-12) took us from 20 to 30 reads/sec. Note the fill model cannot discriminate two books in the same band, so its realizable column will read ~the same for both — a known blind spot here, not a pass. **[2026-08-14 VERDICT — RETIRED, MEASURED AND FAILED]** Against `mmsell10` over each book's own window: depth **150 → +1.34¢**, **225 (`mmsell10d`) → +0.70¢** (n=243, −0.64¢ vs control), **300 (`mmsell10e`) → −1.05¢** (n=133, −1.94¢ vs control). Monotonic decay into negative territory. `mmsell10d` reached its n≥200 and **failed the KEEP clause** (−0.64¢ against a −0.5¢ tolerance); it did not trip KILL (>1.0¢ below), so the gate has a gap there — but it **also lost on TOTAL dollars**, which was the asymmetric escape hatch this gate was built with: same window, **243 × +0.70¢ = +$1.70** against the control's **177 × +1.34¢ = +$2.37**. Sixty-six more trades, less money; the increment (ranks 150–225) runs about **−1.0¢/trade**. `mmsell10e` is retired below its n on a clause that does not depend on n — negative in absolute terms — since no plausible remaining sample closes a 1.94¢ gap. **Mechanism:** the volume rank cut selects for LIQUID events, and liquidity is exactly what a resting maker needs to fill at all and to avoid being picked off; the cap was doing work, not limiting us. **Rate limits were NOT the constraint** — 429s have been zero since the `advanced` grant, measured through the 225→300 step that doubled fetch load. Retiring both returns the shared scan depth to the global 150 (halving orderbook fetches) — a side benefit, not the reason. **This result also CLOSES the inline quote pre-filter** (`docs/MMSELL_QUOTE_PARITY.md`), whose only surviving justification was affording deeper scanning. **Revive only on a mechanically different (taker) entry** — more paper on this entry would just restate the same answer more confidently. |
| `pin15` | **RETIRED 2026-07-16** (`pin15_enabled=False`) | `docs/PIN15_THESIS.md` | 15-min crypto endgame observation-pin: taker-buy the drift-favored side 2–3 min before close, held to the 60s-average settle | Gate FAILED at n=405: T-at-entry slice (by `fill_assumption`) shows the target T≈120–180s window nets only **+0.27¢/trade** (< the +1.5¢ bar); the whole book's cumulative loss traces to the T 60–120s sub-window (**−53¢/trade**, n=37). Even excluding that sub-window stays sub-bar. See `docs/RESEARCH_JOURNAL.md` (2026-07-16 verdict). |
| `theta4` | **LIVE** since 2026-07-30 (Stage 1 pilot, `LIVE_STRATEGIES=theta4`; paper twin `theta4_pt3`; rest of theta family collect-only) | `docs/THETA_THESIS.md`, `docs/THETA_FILL_MODEL.md`, `docs/THETA_LIVE_PLAN.md` | fat-tail (×2.0) revival of theta tail-sell, edge=6¢, cheap band / final 35 min | at **n≥80 settled**, KEEP only if per-trade **> +0.87¢** (was "> 0" — see the FEE RE-BASELINE section above; the old bar was stated against a simulator that overcharged maker entries by that much, so "> 0" today is a strictly weaker test than the one intended) AND realized-tail-hit **≤ 1.25× modeled**; fail both → theta family fully dead. **Paper gate CLEARED 2026-07-28 (n=95, holding at n=114: +$0.392/trade, +$44.70 total), but this uses the identical maker-sell convention as mmsell — gate on `theta fill model`'s realizable ¢/trade too, not the paper gate alone (borrowed-calibration first read: +0.51¢ realizable at 28% coverage vs +38.62¢ optimistic).** `LiveExecutor.mirror_theta_entry` + a `theta4_pt` live/paper twin now exist (`docs/THETA_LIVE_PLAN.md`) to replace the borrowed calibration with theta's own ground truth — see that doc's pre-registered Stage 1/2 gates before arming `LIVE_STRATEGIES=theta4`. |
| `theta`, `theta1`, `theta2`, `theta3` | paper, **collect-only (shelved)** | `docs/THETA_THESIS.md` | model-anchored crypto tail-sell (control + revisions) | SHELVED 2026-07-09 (failed "positive AND calibrated at n≥60"); collectors kept, entries off |
| `mmsell` | **paper — CONTROL BOOK, not a promotion candidate** | `docs/RESEARCH_JOURNAL.md`, `docs/edge_research.md`, `docs/MMSELL_LIVE_PLAN.md`, `docs/MMSELL_LIVE_POSTMORTEM.md` | favorite-longshot maker-sell in the cheap band (mostly sports) | `mmsell3` passed its paper gate, went LIVE 2026-07-13, **CLOSED LIVE 2026-07-19** (final n=359, 91.1% win, +0.18¢/trade realized ≈ breakeven; loss engine outlived World Cup, reattaching to MLB/tennis/cricket h2h-winners) — see `docs/MMSELL_LIVE_POSTMORTEM.md`. **Divergence root-caused (`docs/MMSELL_FILL_MODEL.md`): maker adverse selection — paper counts the +3.77¢ winners a resting order can't fill.** Under the fill model paper `mmsell3` reads −1.06¢ realizable (MIRAGE), matching the live loss. Paper `mmsell3` + variants continue; re-live-test only from the cheap fillable band (mmsell10 regime). **Exit-rule study now runs across ALL mmsell books** (`mmsell exit study`, `docs/MMSELL_EXIT_STUDY.md`): per-cycle price capture (`mmsell_position_ticks`) feeds a counterfactual stop-loss / volatility-exit replay vs hold-to-settlement — gate a rule on Δp5-tail-up AND Δmean ≥ −0.3¢ at n≥100 replayable **[2026-08-12] KEPT DELIBERATELY.** `mmsell` is the wide-band reference the scan funnel's `in_band` counter is scoped to, and the baseline every band/type experiment was defined against. It is not a live candidate and never was (−1.80¢/trade, ≈−0.93¢ after the fee correction) — it is the measuring stick, and deleting it would silently rebase the telemetry rather than save anything. |
| `mmsell1`, `mmsell2`, `mmsell3` | **RETIRED 2026-08-12** (stopped opening 2026-08-12; open positions settled out normally) | `docs/MMSELL_VARIANTS_THESIS.md`, `docs/MMSELL_LIVE_POSTMORTEM.md` | first-cohort band variants (lo=5,hi=20 / lo=10,hi=20 / lo=5,hi=10) | **RETIRED — breakeven, and superseded by `mmsell10`.** Under the fee-corrected fill model `mmsell3` reads **+0.02¢/contract** realizable; `mmsell1` and `mmsell2` sit at 49.6% and **19.5%** fill coverage, i.e. most of their trades are at prices we have no live fill evidence for. `mmsell3` is the one book with live ground truth — it traded real money 2026-07-13→19 and realized **+0.18¢/trade at n=359**, which is breakeven, not edge (`docs/MMSELL_LIVE_POSTMORTEM.md`). The price-ceiling successor `mmsell10` covers the same flow at 99.8% coverage and +2.19¢ realizable. **Revive only** on a mechanically different entry, not on a re-read of this data. |
| `mmsell5`, `mmsell6`, `mmsell7`, `mmsell8` | **paper** (built 2026-07-15) | `docs/MMSELL_VARIANTS_THESIS.md`, `docs/MMSELL_FILL_MODEL.md` | 5 variants from the live+paper by-sport / by-market-type decomposition (clean-book, type-allowlist, ultra-cheap, short-dated, scheduled-settle) | **gate on REALIZABLE (live-calibrated) per-trade — `mmsell fill model`, not blended paper** (see `docs/MMSELL_FILL_MODEL.md`; blended paper overstates edge by the maker adverse-selection gap). mmsell6 was a blended-paper promote candidate but is a **MIRAGE** under the fill model (+1.63¢ opt → −0.43¢ realizable). n≥150 (mmsell5/8 n≥100); full per-variant gates in the thesis doc **[2026-08-12] `mmsell4` RETIRED** from this cohort (+0.04¢ realizable — the skip-list variant never separated from its parent). `mmsell6`/`mmsell7` are KEPT but UNDECIDED at +0.64¢/+0.59¢ fee-corrected: genuinely marginal, not yet failing. |
| `mmsell9`, `mmsell10` | **paper** (built 2026-07-18) | `docs/MMSELL_VARIANTS_THESIS.md` (2nd cohort), `docs/MMSELL_FILL_MODEL.md` | from the live 2×2 (price×type, n=232): cheap (yes≤7¢) × non-winner is the +EV cell; rich h2h-winners are the drag. mmsell9=sweet-spot cell, mmsell10=entry-price ceiling only (`maxyes`), mmsell11=no-late-entry (`htcmin=6`) | **gate on REALIZABLE per-trade (`mmsell fill model`).** Under the fill model **`mmsell10` is the one true live candidate** (100% coverage, +3.80¢ opt → **+1.40¢ realizable** — the price ceiling keeps the fillable cheap cells); **mmsell11 is a MIRAGE** (+2.38¢ opt → −0.86¢ realizable). mmsell10 promotes straight into the live mmsell3 entry as a `maxyes` cap once n clears **[2026-08-12] `mmsell11` RETIRED** (+0.04¢ realizable): the no-late-entry filter (`htcmin=6`) cost flow without moving per-trade, which is the MIRAGE verdict this row already predicted, now confirmed against the corrected fee model. |
| `Lmmsell8`, `Lmmsell10` | **LIVE** since 2026-08-15 (`LIVE_STRATEGIES=theta4,Lmmsell8,Lmmsell10`; twins `Lmmsell8_pt3`/`Lmmsell10_pt3`) | `docs/LIVE_PAPER_TWIN.md` §5b, `docs/MMSELL_VARIANTS_THESIS.md` | Byte-identical replicas of `mmsell8` / `mmsell10`, armed live INSTEAD of their parents. They exist only so live starts with a clean position book: the live mirror fires from the paper-open branch, so a tag whose paper positions predate arming hands live tickers it can never trade (measured 2026-08-15: `mmsell10` had **87** such positions vs `mmsell8`'s **3** — throttling the CONTROL ~29x harder than the treatment). Parents keep running as paper and remain the registry's long-run control series. | The pair IS the experiment: **`Lmmsell8` is the treatment, `Lmmsell10` the control**, armed the same instant at the same $2 / 2-contract clip, so the difference is `mmsell8`'s scheduled-settle allowlist alone. `Lmmsell8`'s thesis is a FILL claim (no in-play informed flow -> no adverse selection), which paper cannot evaluate — the fill model projects both to ~+2.17¢ because it sees entry price only. So gate on the LIVE read: at **n≥150 settled each**, KEEP `Lmmsell8` only if its live win% tracks its twin's within **1pp** AND its live ¢/contract beats `Lmmsell10`'s by **≥+1.0¢**. If both books show the same live-vs-twin gap, the scheduled-settle thesis is dead and `mmsell8` is `mmsell10` with a narrower ticker list. Read per-contract, not volume: the parents' legacy lockout means early n is not comparable across the pair. |
| `mmsell10a`, `mmsell10b` | **RETIRED FROM LIVE 2026-08-14** — live 2026-08-04 → 08-14; open positions left to settle naturally. `LIVE_STRATEGIES=theta4`, `MMSELL_LIVE_OFFSET_AB_ARMS=""` | `docs/MMSELL_OFFSET_AB.md` | queue-position A/B on the `mmsell10` entry (`lo=5,hi=10,maxyes=7`), 1-contract clips: **10a rests AT the no-bid (control), 10b rests 1¢ better**. Each ticker is assigned to exactly one arm by a deterministic hash, so the two books never contest a ticker and the split is random rather than decided by book order — `live_open_order_exists` is strategy-agnostic, so without this they would be split by a race. Tests the only untested live lever on the ~2¢ maker adverse-selection gap. | at **n≥150 settled contracts per arm**, PROMOTE the 1¢ offset only if `mmsell10b` beats **`mmsell10a`** (NOT `mmsell10` — different flow slice and clip size) by **≥0.5¢/contract**; KILL at or below `mmsell10a`; NO if positive but under +0.5¢. Read `mmsell_offset_ab`; verify the arms' avg fill prices differ by ~1¢ and their order counts are within ~10%, else the experiment is not running. Each has its own auto twin (`_pt`) for `live_paper_parity`. | **[2026-08-14 VERDICT — KILL the 1¢ offset.]** Gate is `gap = twin ¢/ct − live ¢/ct`, KILL when `gap_b ≥ gap_a`. Measured: **`mmsell10a` gap −0.06¢** (twin +0.78 vs live **+0.84**, n=420 — live *beat* its twin) versus **`mmsell10b` gap +3.45¢** (twin +0.16 vs live **−3.29**, n=384). `gap_b ≥ gap_a` by **3.51¢** — KILL, not marginally. **The offset worked mechanically and that is what killed it:** fill rate 58.2% vs 55.3%, at-the-front-of-queue **77.0% vs 35.9%** (measured directly via `live_order_queue_ticks`, `docs/LIVE_QUEUE_POSITION.md`), and live win% collapsing **−4.8pp** against its own twin versus −1.4pp for the control. That is this doc's pre-registered failure signature — a higher fill rate with worse realized P&L is bought adverse selection. The cent gets you to the front of the queue; the front of the queue is where the losers cross into you. **Two honesty notes:** (1) `mmsell10b` was killed at **n=384 against its own n≥400 bar** (96%) because the operator ended live mmsell trading entirely — the direction is unambiguous at a 3.51¢ margin, but the bar was NOT formally cleared and nothing may later be promoted on a claim that it was; (2) the between-arm P&L race never became decidable and never could have (−2.77¢, 95% CI [−7.09, +1.54], needs ~30,391 contracts/arm) — which the doc predicted, and is why the twin-paired read was pre-registered as primary. **`mmsell10a` was profitable when retired** (+0.84¢/ct live at n=420, execution matching paper) — it was ended by operator decision to clear the deck for new strategies, NOT because it failed. Revive it as the maker control if a future live maker test needs one. **Do not re-arm `MMSELL_LIVE_OFFSET_AB_ARMS`:** non-empty, `maker_offset` silently splits ANY live mmsell book across the arms by ticker hash, so a future book would inherit the losing treatment without anyone choosing it. |
| `mmsellA1`, `mmsellA2`, `mmsellA3` | **RETIRED 2026-08-12** (anchor set, built 2026-07-30) | `docs/MMSELL_ANCHOR_SET.md`, `docs/MMSELL_CRYPTO_STUDY.md`, `docs/MMSELL_ROADMAP.md` §10 | stop-loss LEVEL sweep on the mmsell10 base (`lo=5,hi=10,maxyes=7`): exit the short when the **yes-BID** ≥ 12 / 20 / 30¢ for K=2 consecutive cycles. Trigger is the bid, never mid/ask (wide books make the mid a fiction — mid/ask stops fired ~100% of the time for −74 to −91¢). Backtest optimum sat at L12–L15, *tighter* than predicted. | **RETIRED — the pre-registered gate FAILED, on the half that mattered.** Read as `status IN ('settled','closed_sl')`, all three stop levels are strongly negative AND the stop makes the 5th-pctile tail WORSE than holding — it fires on **52%** of positions, so it converts small wins into realized losses far more often than it truncates a real tail. A1 −4.16¢/trade vs the `mmsell10` control's +3.14¢; p5 −19.0 vs +5.0. This is a RELATIVE gate, so the 2026-08-11 fee correction does not touch it. The crypto backtest that motivated these did not transfer: it was measured on `htc<1h` continuous-path crypto, while mmsell trades `htc≥1h` sports that JUMP. **Revive only** with a stop whose trigger cannot fire on a non-informative quote, and fresh pre-registration. at **n≥100 settled**, PROMOTE only the single best level, and only if 5th-pctile P&L is clearly above the `mmsell10` control **AND** mean ≥ control − 0.3¢/trade. KILL a level at mean ≥ 1.0¢/trade below control, or tail no better than control. Backtest population was `htc<1h` crypto vs mmsell's `htc≥1h` — this is the out-of-sample test. **Live still additionally requires `mmsell fill model` realizable ¢/trade**, and the taker exit cost is not yet modeled in paper. **[2026-08-03] READING CORRECTION: score these on `status IN ('settled','closed_sl')` — filtering to `settled` alone silently drops every position the stop actually closed, i.e. exactly the trades that measure the stop. Under the corrected read at n=27–44 all three levels are strongly negative and FAIL both halves of the gate (A1 −4.16¢/trade vs the mmsell10 control's +3.14¢, and p5 −19.0 vs the control's +5.0 — the stop makes the 5th-pctile tail WORSE because it fires on 52% of positions). Directional, not final, at n<100 — see `docs/MMSELL_ROADMAP.md` §10.** |
| `mmsellA4` | **paper only** (anchor set, built 2026-07-30) | `docs/MMSELL_ANCHOR_SET.md` | volatility ENTRY gate on the mmsell10 base: skip the entry if the yes-mid range over the last 6 recorded candidate ticks ≥ 6¢. Backtest: calm tape +2.85 to +5.25¢ at 100% win, active tape −39¢, but only n=13–17. Does **not** fire on <3 ticks, so the book differs from control only by entries the gate genuinely rejected. | at **n≥100 settled**, KEEP only if mean/trade beats the `mmsell10` control by ≥ 1.0¢ **AND** the gate rejected ≥ 15% of candidates (`skipped_vol_gate`). KILL if mean ≤ control, or rejection rate < 5% (gate inert → book is a duplicate of mmsell10). **[2026-08-13 GATE READING — both halves now measurable, both PASS; not yet a promotion.]** Same-window read restricted to post-fee-fix trades (>= 2026-08-11 15:00 UTC, the measured deploy boundary): `mmsellA4` **+4.05¢/trade at n=128** vs the `mmsell10` control's **+2.75¢ at n=162** — beats control by **+1.30¢**, clearing the ≥+1.0¢ half. Rejection rate is **23.6%** (control took 534 entries in the window, A4 took 408), clearing the ≥15% half — the gate is firing, so the book is genuinely not a duplicate of its control. Measured by entry-count differential because `skipped_vol_gate` is counted in `MmsellCycleSummary` but never logged or persisted, so it cannot be queried; the differential is valid only while A4 and mmsell10 differ ONLY by the gate (true as built). Held short of a KEEP verdict deliberately: n=128 on ~2 days of post-boundary data, and this is a RELATIVE gate whose control is itself moving. Re-read at n≥300 before promoting. |
| `mmsellA5` | **paper only** (anchor set, built 2026-07-30; **n=0 until the 2026-08-03 gate fix**; pairing fixed 2026-08-14 — see below) | `docs/MMSELL_ANCHOR_SET.md` | short strangle: sell BOTH mutually-exclusive cheap tails of one event (cheap YES on a high strike + cheap NO on a low strike, the mirror band). Two premiums against at most one loss — one settlement can never lose both legs. Only entered when the event actually has both tails under the cap; a lone tail is refused (it would be a duplicate mmsell10 trade). | at **n≥82 clean pairs**, KEEP only if the 95% lower bound on the pair win rate clears **93.9%** (the 7¢ break-even) — the exact bound the n=23 backtest failed at 87.8% despite +3.30¢/pair and 100% win. KILL if the bound fails at n≥82, or paired entry rate can't reach 82 pairs in a quarter. Caveat: both-tails-cheap selects for low-volatility events, so A5 and A4 are probably **not** independent edges. **Its n starts at 2026-08-03:** the pairing gate read the integer-cent quote keys, which the live events payload omits (it sends `*_dollars`), so it was always False and A5 never traded. Still n=0 a few checks after that deploy = another bug, not selectivity. **A second bug found 2026-08-14 once NFL supply exposed it:** `_event_has_both_tails` certifies the EVENT, not a matched PAIR — on a multi-strike ladder (`KXNFLSPREAD`/`KXNFLTOTAL`) several markets could independently clear the same side's band, so one event opened 4 correlated cheap-NO legs and 0 cheap-YES legs; a pairing audit found only 27% of events (192/715) had actually taken both sides. Fixed by capping entry to one leg per side per event (`MmSellTracker._strangle_leg_taken`). **PAIRING BOUNDARY 2026-08-14T14:31:12Z** (PR #213 merge — provisional, see `docs/MMSELL_ANCHOR_SET.md` for the empirical-confirmation status): pre-boundary volume is real P&L but not clean pair evidence — any pair-count or pair-win-rate-bound read toward n≥82 MUST filter `created_at >= '2026-08-14T14:31:12Z'`, the same hard-floor remedy as the 2026-08-13 UNIVERSE BOUNDARY above (no offset converts a one-sided-legs sample into a paired one). See `docs/MMSELL_ROADMAP.md` §10a: with stops, vol gates, late adds and per-event caps all measured dead, the strangle is the only **structural** tail reducer left in the design — it improves the payoff *shape* (two premiums, at most one loss) instead of trading mean for variance, which is why it matters more than its n=23 backtest suggested. |
| `Wmmsell1`–`Wmmsell8` | **RETIRED 2026-08-12** (market-type set, built 2026-08-03) | `docs/MMSELL_TYPE_BOOKS.md`, `docs/MMSELL_MARKET_TYPES.md` | WIDE band (`lo=5,hi=40`, no `maxyes`) sliced by CONTRACT STRUCTURE — the axis every previous mmsell read was blind to. W1=in-play only, W2=the 5 positive-edge in-play types, W3=the concentrated 3, W4=price_strike, W5=mention, W6=all-minus-the-5-negative-types, W7=the no-clock complement of W1, W8=high-conviction (edge ≥+3.0pp AND n≥500) | **RETIRED as UNMEASURABLE — not disproven.** Two independent reasons, and the second is the structural one. (1) Their control `mmsell` LOSES money (−1.80¢/trade, ≈−0.93¢ fee-corrected), and beating a losing control is not an edge — the 2026-08-09 absolute floor was added for exactly this and five W books failed it. (2) Fill coverage is **19–41%**: the live fill calibration comes from cheap-band maker orders, so the wide band's 10–40¢ entries have NO live fill evidence at all. We cannot say whether a resting order there would ever fill, which makes their realizable number an estimate over a minority of each book. The tight-band `Tmmsell*` family tests the SAME contract-type hypothesis at 99–100% coverage, so the axis survives — only the band dies. **Revive** if we ever obtain live maker fills in the 10–40¢ band; that evidence, not more paper, is the blocker. **`mmsell` (the control book) is the control** — each W book differs from it ONLY by the type filter, so read them against it over the SAME window, never in absolute terms. At **n≥150 settled**, KEEP only if the book's own ¢/trade is **> 0 absolute** AND it beats the control by **≥+1.0¢** AND `mmsell fill model` realizable ¢/trade **> 0**. KILL if ≤ control, or ≤ 0 absolute, or realizable ≤ 0. **[2026-08-09 GATE FIX]** the absolute-floor clause was added after the scan fix left the wide control `mmsell` at **−1.80¢/trade**: five W books cleared the relative bar while three were still LOSING money. Beating a losing control is not an edge — read the wide band as dead, not as five passes. Caveat pre-registered in the thesis doc: the filters were derived from the same history they are first measured against, so the census cells are a PRIOR — the test that counts is out-of-sample persistence through the Sept–Nov regime change. **[2026-08-11 FEE RE-BASELINE]** both ABSOLUTE clauses above (own ¢/trade, realizable) are fee-sensitive and were written against a simulator that overcharged maker entries by ~0.87¢/contract — on pre-2026-08-11 trades read each `> 0` as **`> +0.87¢`**; post-boundary trades use `> 0` as written. The RELATIVE clause is unaffected (control and book shift together). The 2026-08-09 verdict SURVIVES the correction: the wide control was −1.80¢/trade, ≈−0.93¢ corrected — still losing, so the wide band is still dead. |
| `Tmmsell1`, `Tmmsell2`, `Tmmsell5`, `Tmmsell6` | **paper only** (market-type set, built 2026-08-03) — **`Tmmsell3`, `Tmmsell4` RETIRED 2026-08-12** — **COHORT RESTARTED 2026-08-13 18:09:40Z** | `docs/MMSELL_TYPE_BOOKS.md`, `docs/MMSELL_MARKET_TYPES.md` | TIGHT band (`lo=5,hi=10,maxyes=7`) sliced by the same taxonomy. T1=price_strike, T2=mention, T3=the top-3 in-play types by tight-band edge, T4=all-minus-the-5-negative, T5=the no-clock book, T6=both-band survivors (positive edge in BOTH price bands) | **`mmsell10` is the control** (same band, no type filter). At **n≥100 settled**, KEEP only if the book's own ¢/trade is **> 0 absolute** AND it beats `mmsell10` by **≥+1.0¢** over the same window AND realizable ¢/trade **> 0**; KILL if ≤ control or ≤ 0 absolute. **[2026-08-09]** absolute floor added — see the Wmmsell row. The two families exist separately because the type ranking INVERTS across the bands — `total` is −0.4pp wide / **+3.5pp** tight, `h2h` +0.7pp wide / **−0.5pp** tight, `game_prop` +4.2pp wide / **−0.6pp** tight — so a type verdict from one band says nothing about the other. Narrow books (T1/T2) may need a quarter to reach n; early emptiness is flow, not a verdict. **[2026-08-11 FEE RE-BASELINE]** both ABSOLUTE clauses above (own ¢/trade, realizable) are fee-sensitive and were written against a simulator that overcharged maker entries by ~0.87¢/contract — on pre-2026-08-11 trades read each `> 0` as **`> +0.87¢`**; post-boundary trades use `> 0` as written. The RELATIVE clause is unaffected (control and book shift together). The 2026-08-09 verdict SURVIVES the correction: the wide control was −1.80¢/trade, ≈−0.93¢ corrected — still losing, so the wide band is still dead. | **[2026-08-12 VERDICT]** `Tmmsell3`/`Tmmsell4` retired: both reached n and beat `mmsell10` by far less than the gate's **+1.0¢**. Retired as MEASURED AND FAILED — unlike the wide band's UNMEASURABLE verdict, which licenses a revival and this does not. The relative gate is untouched by the 2026-08-11 maker-fee correction (both sides are maker books at the same clip, so the fee cancels in the difference), which is why they are retired on the relative clause rather than the absolute one. **The family result is bigger than either book:** every T book that reached n beat the control, none by the gate's margin — once you are already selling ≤7¢ tails, selecting on contract structure adds ~nothing, and the price band was doing the work the type filter was credited with. **Do not promote a T book on realizable ¢/trade:** the fill model projects entry price only, so every tight-band book INCLUDING the control lands at roughly the same number and condition 3 does no discriminating work. Survivors kept for the Sept–Nov regime change; `Tmmsell1`/`2`/`5` are flow-constrained, not failing. Revival requires clearing +1.0¢ in a DIFFERENT regime. See `docs/MMSELL_TYPE_BOOKS.md`. **[2026-08-13 COHORT BOUNDARY]** the taxonomy deploy took classified coverage from 50.5% to **99.6%** of candidate flow (46 series added, none reclassified), and these four select via `mtype=`/`mode=` — an unclassified series is admitted by NO allowlist, so until 18:09:40Z each was being offered roughly HALF its intended universe, and specifically the half the census prior was fit on. Entry rates jumped **5–9×** across the instant while the un-filtered control `mmsell10` moved 1.6× with the ambient universe. **`n` restarts at 0 here and pre-boundary trades are DROPPED, not adjusted** — unlike the fee boundary this is a universe change, and no offset converts one population into the other (see the UNIVERSE BOUNDARIES section above). The tags deliberately did NOT change: the selection rule is byte-identical either side, so a fresh tag would assert a change that did not happen. `mmsell_fill_model` enforces the floor and prints the matched-window control. **The upside:** the 46 new series were never in the census, so post-boundary flow is the first genuinely OUT-OF-SAMPLE test this family has had — which is exactly what its "honest caveat" said it needed. |
| `weather_con` | **RETIRED 2026-08-12** | `docs/RESEARCH_JOURNAL.md` | synoptic-temperature consensus pick | **RETIRED at n=775: −3.50¢/trade, −$27.14, 34.7% win.** The last weather book still entering; every other cell was pruned by 2026-07-04. Weather books are TAKERS, so the 2026-08-11 maker-fee correction does NOT flatter or excuse this — the number is real. The consensus DATA flags (ensemble, obs, polymarket) stay ON: they feed `weather_forecast_outcomes`, the validation dataset, which is not a book. |
| `weather_concity` | **RETIRED 2026-08-12** | `docs/RESEARCH_JOURNAL.md` | con restricted to the three by-city edge cells | **RETIRED — and it ANSWERED its question, in the negative.** The hypothesis was that con's loss is diluted by bad cities, so restricting to the three by-city edge cells would turn it positive. Measured at n=203 on the same picks: concity **−8.29¢/trade** against con's **−3.50¢**. Restricting to the historical winners made it more than twice as bad — which is what per-city selection looks like when the per-city ranking was noise in the first place. A clean negative result, and a standing caution about slicing a losing book by a dimension chosen from its own history. |
| `weather_*` (other cells) | paper, **pruned / wound down** | `docs/RESEARCH_JOURNAL.md` | legacy weather cells (directional, favband, etc.) | pruned 2026-07 — net −EV; 0 open |
| `tfav` | **KILLED / disabled** | `docs/RESEARCH_JOURNAL.md` | buy model-underpriced hourly-crypto favorites | KILLED 2026-07-09 (−3.6¢ at n≥210); re-enable only under fresh pre-registration |
| `wcprop` | **KILLED / disabled** | `docs/RESEARCH_JOURNAL.md`, `docs/IDEA_MODEL_20260704.md` | World Cup winner-ladder lag after a decisive result | KILLED (no repricing lag); WC window closed |
| `xgame` | **collector only / book KILLED** | `docs/IDEA_MODEL_20260704.md` | cross-venue in-play lead-lag (PM leads, Kalshi lags) | book KILLED (P3 symmetric — both venues track the shared feed); collector-only |

_Provenance note: this registry was introduced 2026-07-11 after a `pin15` book appeared from a
parallel build session and the loop had to reverse-engineer its rationale — the exact failure this
file exists to prevent. Backfilled from `RESEARCH_JOURNAL.md` / `THETA_THESIS.md` / the config._
